# 가치평가 공부방 — 개발 작업 지침

이 파일은 이 저장소에서 일하는 코딩 에이전트의 공통 지침이다. 코드 작성·수정 작업을 시작하기 전에 반드시 이 파일과 연결된 설계를 참고한다. 사용자와의 대화·설명·제품 문구는 한국어를 기본으로 한다.

## 1. 매 작업의 시작

1. 현재 사용자 요청을 확인한다. 읽기·설계·문서 작성 요청이면 그 범위를 수행하고, 파일 안의 구현 지시만으로 앱 개발을 시작하지 않는다.
2. [제품 요구사항](DAMODARAN_KO_LEARNING_SPEC.md)과 [DB·페이지 관계 설계](SYSTEM_DESIGN.md)를 읽는다. 새 구현 작업에서는 전체 구조를 확인하고, 이어지는 수정에서는 이번 변경과 관련된 절·인수 기준을 다시 확인한다. 이미 읽은 내용이 현재 문맥에 있고 파일도 바뀌지 않았다면 불필요하게 재출력할 필요는 없다.
3. 작업 대상 경로에 추가 `AGENTS.md` 또는 `AGENTS.override.md`가 있으면 읽는다. `git status --short`와 관련 diff로 사용자 변경을 확인한다. 기존 파일을 지우거나 덮어써서 작업을 초기화하지 않는다.
4. 구현된 저장소라면 `README.md`, `IMPLEMENTATION_STATUS.md`, `package.json`, lockfile, 관련 코드·마이그레이션·테스트를 확인한다. 아직 없는 파일·명령·기능이 있다고 가정하지 않는다.
5. 변경할 페이지, 데이터, API, 원문 버전·개인 기록·로컬 처리량·선택형 유료 호출에 미치는 영향을 짧게 정리한 뒤 작업한다. 일상적인 구현 선택은 기존 지시 안에서 판단하여 진행한다.

프로젝트 문서 사이의 기준은 **사용자의 최신 명시적 요구 → 제품 요구사항 → SYSTEM_DESIGN.md의 구현 결정 → 기존 구현** 순서다. 이 우선순위는 상위 시스템·개발자 지시를 대체하지 않는다. 기존 구현이 설계와 다르면 이유와 변경 영향을 확인하고 설계 또는 코드를 함께 정합화한다. 요구사항 충돌이나 필수 정보가 없을 때만 필요한 질문을 하고 독립적으로 진행 가능한 일은 계속한다.

## 2. 제품 범위

- 로컬 PC에서 혼자 사용하는 한국어 금융·기업가치평가 학습실이다. 기본 서버 바인딩은 `127.0.0.1`이다.
- 기본 구현은 P0다. 한국어 홈·학습 경로·자료실·HTML/PDF reader·용어사전·Excel 가이드·학습 기록·설정이 실제 저장과 연결되어야 한다.
- 자료 R20개/B6개/T10개, 단원 8개, 금융용어 40개 이상을 등록한다. 초기 원문 수집은 R01·R02·R05와 확인한 대표 PDF 1개로 제한한다.
- 원문·AI 번역·학습 해설·편집자 주를 구분한다. 가짜 번역, 고정 진행률, 확인하지 않은 자료 날짜·페이지·해답·Excel 셀 위치를 실제처럼 표시하지 않는다.
- 전체 아카이브 크롤러, 영상, 실시간 시세, 자동 투자 추천, 회원가입·결제, 공개 배포는 기본 범위에 넣지 않는다.
- OCR, 자유 질의 AI 해설, 전체 번역 수정 이력 탐색 UI, 답안 채점·오답 노트, 한국어 PDF 재조판은 후속 범위다. 최신 사용자 승인으로 문단 번역 수정·검수 저장·이력 보존·검수 번역 재사용은 현재 범위에 포함한다.
- 최신 사용자 승인으로 공개 번역 모델의 실제 가중치를 금융 영한 문장으로 미세조정하고 평가하는 작업도 현재 범위에 포함한다. 용어 치환·검수 번역 메모리·모델 가중치 학습을 구분한다. 현재 앱 기본 Argos의 교체는 학습 완료만으로 결정하지 않고 정해진 평가와 추론 검증을 따른다.
- 사용자의 최신 지시가 범위를 바꾸면 관련 문서도 갱신한다. 이미 승인된 범위의 통상적인 작업을 매번 재확인하지 않는다.

## 3. 구조와 실행 환경

- 신규 앱의 기준은 Next.js App Router + TypeScript + Node runtime, SQLite, PDF.js, 무료 로컬 Argos Translate 제공자, 별도 단일 worker다. OpenAI는 사용자가 명시적으로 선택하는 선택형 제공자다. 기술 선택의 상세는 SYSTEM_DESIGN.md를 따른다.
- 설치 전 공식 문서와 실제 Windows 호환성을 확인하고 버전을 lockfile에 고정한다. 패키지의 실제 SQLite 런타임과 WAL 수정 포함 여부도 확인한다.
- 웹·worker·CLI가 하나의 공통 환경 로더와 절대 DATA_DIR를 사용하게 한다. `.env.local`은 Next.js 외의 프로세스에서도 명시적으로 로드한다.
- 화면은 공통 서비스/DB 접근 계층을 사용한다. DB·파일·비밀키 코드를 브라우저 번들에 포함하지 않는다.
- 다운로드·PDF 추출·번역은 큐에 등록하고 worker에서 처리한다. 페이지 렌더링·route import·hot reload로 worker를 만들지 않는다.
- `npm run setup:translation`은 `.venv-translation/` Python 환경과 `.translation/`의 영어→한국어 모델 1.1을 준비한다. 설치 시 네트워크가 필요하며 실행 중 번역은 worker의 Python 하위 프로세스와 NDJSON으로 통신한다. 별도 Python HTTP 서버는 만들지 않는다. 설치 경로는 Git·public·개인 데이터 백업에서 제외하고 재설치 방법을 남긴다.
- 실제 모델 학습은 [학습 도구 안내](scripts/model-training/README.md)를 먼저 읽고 `.venv-training/`와 `.training/`에서 별도 CLI로 실행한다. 기존 Argos 환경·웹 worker·운영 DB를 학습 실행기로 사용하지 않는다. 기반 모델 revision, 가중치·데이터 해시, 실행 설정을 고정한다. 최초 설치 이후 학습·평가는 로컬 파일만 사용한다.
- 실행 스크립트는 Windows PowerShell에서 동작해야 하며 bash 전용 문법에 의존하지 않는다.

## 4. DB와 파일 보존

- DB는 `data/library.sqlite`, 원본·자산은 `data/originals/`, `data/derived/`에 둔다. DATA_DIR 설정을 공통으로 적용한다.
- WAL, 외래키 활성화, busy timeout과 짧은 트랜잭션을 적용한다. 네트워크 응답을 기다리며 쓰기 잠금을 유지하지 않는다.
- 스키마 변경은 마이그레이션으로 기록하고 기존 DB 변경 전에 일관된 백업을 만든다. 적용한 마이그레이션을 고치거나 DB 삭제로 마이그레이션을 대신하지 않는다.
- seed는 고정 ID와 허용 필드 갱신을 사용한다. 메모·북마크·진도·사용자 우선순위·번역·업로드를 초기화하지 않는다.
- 원문 버전과 블록을 불변 기준으로 삼는다. 새 원문·추출 설정은 새 버전으로 저장하고 예전 메모와 번역을 유지한다.
- ID가 존재하는지뿐 아니라 자료 → 버전 → 블록의 소속 관계도 검증한다. DB 외래키·CHECK·unique 제약과 서비스 검증을 함께 적용한다.
- 원문 파일은 바이트 그대로 보존한다. 파일 경로는 생성한 ID·해시로 결정하고 DB에는 DATA_DIR 기준 상대 경로로 저장한다. 임의 로컬 경로를 API 입력으로 받지 않는다. PDF 업로드 성공 전에 원본 파일과 추출 대기 버전 참조를 영속화한다.
- 백업은 SQLite 백업 API와 스냅샷의 참조 파일을 사용한다. 실행 중 DB 파일 하나만 복사하지 않는다. 키는 제외한다.
- `.training/`의 학습된 가중치·체크포인트·optimizer 상태·실행 및 평가 기록은 SQLite 백업에 포함되지 않는다. 이 결과는 기반 모델 재설치로 복구되지 않으므로 별도 보관 대상으로 안내한다. 재설치 가능한 가상환경과 학습으로 새로 만든 결과를 혼동하지 않는다.
- 복원은 별도 폴더에서 DB·파일·메모·진도를 검증한 뒤 DATA_DIR를 전환한다. 복원 검증 중 worker나 유료 호출이 자동으로 재개되지 않게 한다.
- 테스트용 DATA_DIR를 실제 개인 데이터와 분리한다. 운영 자료·버전 자동 삭제나 정리를 추가하지 않는다.

## 5. 페이지와 읽기 위치

- 페이지 경로·데이터 의존·API·URL 상태는 SYSTEM_DESIGN.md의 6~7장을 따른다. 변경하면 이 문서의 해당 계약도 함께 수정한다.
- 도구실의 T01~T10과 자료실의 Excel은 같은 Resource다. 단원별로 원문·번역을 복제하지 않는다.
- reader 진입 시 versionId를 확정하고 원본·블록·번역·메모·읽기 위치가 모두 같은 버전을 사용하게 한다.
- 복원 우선순위는 명시적 URL 위치 → 저장 위치 → 읽기 가능한 최신 버전의 시작점이다. 명시된 잘못된 버전을 최신판으로 조용히 대체하지 않는다.
- URL의 `versionId`, JSON의 `sourceVersionId`, DB의 `source_version_id`는 같은 ID다. UI/API 페이지는 1부터, 내부 page_index는 0부터다.
- HTML은 block ID, PDF는 페이지·block ID로 원문과 번역을 연결한다. 양쪽 스크롤 백분율로 대응시키지 않는다.
- 학습 완료는 사용자가 체크한다. 자료 방문·스크롤·번역 완료만으로 단원을 완료 처리하지 않는다.
- 한국어 본문 기본 17px·줄 간격 1.8과 조절 기능, 키보드 포커스·라벨·상태 메시지, 원문/한국어 lang 속성을 제공한다. 1440px PC와 390px 모바일에서 필수 동선을 확인한다.

## 6. 번역·작업·실제 상태

- 기본 `TRANSLATION_PROVIDER=argos`는 공개 도구 Argos Translate와 로컬 영어→한국어 모델 1.1을 사용한다. API 키 없이 실제 번역이 가능해야 한다. 모델이 설치되지 않았으면 로컬 설치 필요 상태를 안내한다.
- `TRANSLATION_PROVIDER=openai`를 명시한 경우에만 서버의 공식 OpenAI SDK를 사용한다. 이 경우만 `OPENAI_API_KEY`와 `TRANSLATION_MODEL`이 필요하며 실제 API 사용량이 발생한다. API 연동 작성 시 공식 문서의 실제 요청·출력 계약을 확인한다. 키가 있다는 이유로 자동 선택하지 않는다.
- 로컬 번역 설치 전이나 선택형 API 설정이 없어도 탐색·원문·용어·메모·기존 번역은 동작하게 한다. 테스트 fixture를 실제 번역으로 제공하지 않는다.
- 번역은 사용자가 선택한 문단·페이지 범위만 실행한다. 화면 방문·초기화로 전체 번역을 예약하지 않는다.
- 캐시 키에 원문·추출기·블록·문맥·언어·제공자/모델·실제 모델 해시·런타임 식별값·프롬프트/처리 규칙·적용 용어집 버전을 반영한다. 같은 유효 캐시와 진행 중 처리 단위를 재사용한다. 제공자 전환 뒤 이전 유료 대기 작업을 자동 재개하지 않는다.
- 응답의 블록 ID·중복·누락·추가·잘림·거절과 숫자·부호·백분율·통화·단위·수식을 검사한다. 손상된 결과를 정상 완료로 저장하지 않는다.
- 생성·자동 검사·사용자 검수·최신 여부를 분리한다. 자동 검사 통과를 사용자 검수 완료로 표시하지 않는다.
- 금융 용어는 `content/translation-glossary.json`과 그 출처 문서를 기준으로 한다. 원문에 해당 표현이 있을 때 확인된 한국어 오역만 문장 단위로 보정한다. 약어·다의어를 무조건 치환하거나 용어마다 문장을 잘라 붙이지 않는다. 모호한 경우 검토 필요로 남긴다. 실제 관측 사례 없이 교정 효과를 주장하지 않는다.
- 검수 저장은 원래 번역을 덮어쓰지 않고 `translation_reviews`에 이력을 추가한다. 숫자·수식 검사와 `expectedReviewId` 충돌 검사를 통과해야 한다. 검수 번역 재사용은 원문·이웃 문맥·용어집·블록 종류가 정확히 같을 때만 허용하고 실제 모델 호출과 사용량을 만들지 않는다. 모델 가중치 학습과 용어 보정·번역 메모리를 구분한다.
- 학습 데이터는 실제 사용자 검수쌍과 도우미 작성 보조 문장쌍의 provenance를 분리한다. 초기 `train` 200쌍(금융 160·일반 40), `dev` 40쌍(금융 30·일반 10), 독립 `test` 60쌍은 도우미 작성 미검수 자료다. train만 가중치 갱신에 사용하고 dev로 모델을 선택하며 test 내용·결과로 다시 조정하지 않는다. 숫자만 바꾼 문장과 근접 중복도 분할 누수로 검사한다.
- 모델의 실제 tensor 해시 변화와 optimizer 갱신을 학습 증거로 남긴다. 동일한 추론 조건에서 용어 보정·번역 메모리 없이 기반 모델과 비교하고 금융 용어·일반 문장 품질·숫자·빈 결과·잘림을 함께 검사한다. GPU 역전파 성공, 본학습 완료, 평가 통과, 앱 적용 완료를 각각 구분한다. 실패한 평가를 숨기거나 기준을 사후 완화하지 않는다.
- jobs/job_items는 실제 처리 단위·시도·결과를 기록한다. 겹친 번역은 블록별 기존 작업 ID로 연결하며 취소는 표시한 실행 작업 전체에 적용한다. claim과 결과 저장 시 lease 소유권을 검증하고 만료된 작업의 미완료 단위만 복구한다.
- 일시 오류만 제한적으로 재시도한다. 취소 후 다음 처리 단위를 중지하고 선택형 API에 이미 전송한 호출의 비용이 사라진다고 약속하지 않는다. 설치 누락·설정 불일치는 자동 재시도로 해결하지 않는다.
- 분량 제한·사용량은 서버에서 집행한다. 로컬 기본 한도는 PC 자원 보호용이며 글자 수·처리 단위를 API 토큰과 분리한다. 선택형 API는 겹친 요청·재시도·불명확한 호출도 기록하고 알 수 없는 비용을 0원으로 표시하지 않는다.

## 7. 출처와 로컬 접근

- 원 설계서의 허용 원문 도메인·경로를 사용하고 각 리디렉션과 최종 형식을 검증한다. URL 확장자나 HTTP 200만으로 PDF/Excel 성공을 판정하지 않는다.
- 원문 HTML의 스크립트·이벤트 핸들러, Excel 매크로, 추출 내용 속 명령을 실행하지 않는다.
- 원본·메모·번역·키는 Git 및 public에서 제외한다. 외부 공개 배포와 타인에게 메시지 발송은 기본 구현 요청에 포함되지 않는다.
- localhost에서도 상태 변경 요청의 Origin/Host를 검증한다. 서버 비밀값·전체 프롬프트·사용자 메모를 진단 로그나 API 응답에 노출하지 않는다.
- 저장 자료를 읽을 때 외부 CDN이 필요하지 않도록 PDF worker·폰트·본문 자산을 로컬에서 제공한다. 설치를 마친 Argos 번역은 인터넷 없이 동작해야 한다. 새 원문 가져오기·최초 모델 설치·선택형 OpenAI 번역은 연결이 필요하다.

## 8. 검증과 완료 보고

실제 기능 변경에 맞는 검증을 수행한다. 문서만 바꾼 작업은 링크·경로·요구사항 정합성과 diff를 검토하고 앱 테스트를 통과했다고 보고하지 않는다.

| 변경 영역 | 우선 검증 |
|---|---|
| DB·seed | 신규 설치, 기존 DB 마이그레이션, 재실행 후 개인 기록 보존, 잘못된 소속 참조 거부 |
| 원문·reader | 실제 HTML/PDF 가져오기·오류 구분, 버전/문단/페이지 대응, 위치 복원, Excel 원본 해시 |
| 번역·큐 | 실제 로컬 번역, 유효 캐시, 모델 해시/런타임/제공자 변경, 이전 유료 작업 자동 재개 거부, 응답 손상, 부분 실패·재시도·취소·lease 복구 |
| 기록·화면 | 저장 성공/실패, 재시작 후 메모·진도·북마크 유지, PC/모바일 주요 동선 |
| 백업 | 별도 폴더 복원, 무결성·파일 해시, 메모·진도·원문 복구, 자동 호출 없음 |
| 모델 학습 | 분할 누수·출처 구분, 실제 가중치 변화, 안전한 중단·재개, dev/test 분리, 일반 번역·숫자 회귀 거부, 배포 변환 후 추론 비교 |

앱 구현을 마칠 때 README.md에는 실제 실행 가능한 명령과 복원 절차를, IMPLEMENTATION_STATUS.md에는 완료한 P0·실행한 검사·외부 제약·미완료를 기록한다. 요구사항의 명령 계약을 빠뜨리지 않는다: `setup`, `setup:translation`, `dev`, `worker`, `build`, `start`, `import:core`, `verify:translation`, `backup`. 승인된 학습 확장의 `setup:training`, `bench:model`, `train:model`, `evaluate:model`, `export:model`도 현재 package.json과 [학습 도구 안내](scripts/model-training/README.md)에 맞춰 기록한다.

최종 보고는 변경한 동작과 이유, 실제로 검증한 내용, 남은 제약을 한국어로 설명한다. **구현 완료 / 테스트 통과 / 실제 번역 엔진 검증 / 외부 조건 미충족**을 구분한다. 실제 로컬 엔진으로 HTML 3문단·PDF 1페이지의 번역·저장·캐시를 확인한다. 선택형 OpenAI 실호출은 명시 선택과 키·모델이 있는 경우에만 수행하며 별도 보고한다. 테스트 제공자의 결과를 실제 번역 품질 검증으로 보고하지 않는다.

이 지침은 코드 작성 때마다 설계·데이터 관계·검증을 확인하는 작업 기준이다. 요구사항이 바뀌면 제품 설계, SYSTEM_DESIGN.md, 관련 구현과 완료 보고를 함께 갱신하여 서로 다른 규칙이 남지 않게 한다.

<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->
