# 구현 상태 — 달모다란 (MoonModaran)

**최신 S5 상태 — 2026-09-20 평가 완료, 기존 등록 유지.** 원9개와 memory-v6 새55개의 답변을 모두 동결하고128질문을 채점했다(정답117·오답11·미해결0). C1·C3의 일반 질문 회귀와 C2의 필수 개선 부재로 적격 후보가 없다. 고정 계약에 따라 S6 확대평가·신규 등록 없이 기존 앱 구성을 유지한다. 지표와 한계는 [개발 비교 결과](content/model-comparison/input-execution-v1/RESULT_20260920.md), 실행·pause 이력은 [재개 기록](content/model-comparison/input-execution-v1/RESUME_20260920.md)을 따른다.

작성일: 2026-09-10. 기준은 [제품 요구사항](DAMODARAN_KO_LEARNING_SPEC.md) P0와 [DB·페이지 설계](SYSTEM_DESIGN.md)다. 실행과 설정은 [README](README.md)를 따른다.

날짜별 기록은 해당 시점의 이력을 보존한다. 아래 과거 기록의 ‘진행 중·미완료·현재’ 표현보다 위 최신 상태와 해당 후속 완료 기록을 우선한다.

## 2026-09-20 후속 — 운영 웹 번역 안정화

B01의 4,742자 HTML 전체가 단일 블록으로 추출된 상태에서 Hy-MT2 7B Q8 CPU 번역을 실행했고, 노트북 덮개로 Modern Standby에 진입한 뒤 `LOCAL_TIMEOUT`과 연속 `LOCAL_CLOSE_TIMEOUT`이 기록됐다. 기존 실패 작업·모델·등록 manifest·번역은 보존하고 `.env.local`의 운영 제공자만 `argos`로 전환했다.

`npm run verify:translation:html`은 실제 R01 HTML 3문단의 번역·저장·캐시를 통과했고, 재기동한 API는 `provider: argos`, `configured: true`, `liveVerified: true`를 반환했다. 같은 B01 4,742자 블록은 웹 API 경로에서 약 23초에 번역을 저장했지만 `intrinsic value`와 `book value` 용어 검사 때문에 `needs_review`로 남겼다. 검토 필요 결과를 정상 캐시로 재사용하지 않는 계약을 확인했으며, 확인용 재요청은 즉시 취소했다. 이 조치는 속도·가용성 복구이며 Argos 결과의 의미 품질 향상이나 사용자 검수 완료를 뜻하지 않는다.

## 2026-09-20 후속 — S5 실제 평가 완료

개발16원문×4구성의64출력에 대해 원문 검토64개·명제192개·용어156출현·질문128개(핵심64개)의 평가를 완료했다. 수집·전체 답변 동결·채점3묶음·병합·정식 보고서의 실제 증거 검증이 통과했다. 답변 재생성이나 판정 기준 완화는 없다. 세 도우미의 직접 원문 채점과7문항 교차 검토, 별도 도우미의 전체 집계·거부 사유 대조를 수행했으며 사람 검수로 표시하지 않는다.

C0/C1/C2/C3의 중요 의미 오류 단위는2/0/2/0, 핵심 명제는46/46/46/47(각48), 질문 정답은30/29/30/28(각32)이다. C1은 새 핵심 명제 손상과 일반 질문 회귀, C2는 필수 개선 부재, C3는 일반 질문 회귀로 탈락했다. `retain_registered_configuration`으로 S6의 기존 등록 유지 결론을 확정하며 독립80단위 평가는 시작하지 않는다. 질문 답변 오답을 번역 오류로 중복 가산하지 않는다. [원장 실제 추가](.training/quality-evaluation/input-preparation-v1/s5-ledger-appended-memory-v6-20260920/append-receipt.json)는128관측 추가·재사용0,836→964사건, 기존 사건 바이트와 번역 검토146개 보존을 확인했다. 원래 실패 상태와 기존 판정은 바꾸지 않았다.

앱 코드·가중치·운영 DB·원문·개인 기록·활성 등록을 변경하지 않았다. 유료 API·가중치 재학습·신규 앱 build/E2E·실제 HTML3문단/PDF1페이지 앱 생성/저장/캐시 검증은 이번에 실행하지 않았다. 기존 모델의 전반적 품질 수용과 실제 학습 효과는 여전히 입증되지 않았다.

## 2026-09-20 이전 관측 — memory-v6 실제 실행 시작

사용자의 현재 자원 실행 요청에 따라 [메모리 시작 정책 v6](content/model-comparison/input-execution-v1/LOCAL_QUESTION_MEMORY_EXECUTION_V6.md)의 시작 physical만7GiB로 조정했다. native/context·runner 합성31개, pipeline19개 및 추가 변조 검사, private 채점 컨트롤러8개 통과 후 새11파일을 동결했다. 직전 가용physical8.37GiB에서 실제 run 명령을 시작했다. 원9개는 변경하지 않고 나머지55개만 생성하며 commit9GiB·관측 상한6GiB·실행 여유1GiB 보호를 유지한다. 실제 생성·채점 결과는 [재개 기록](content/model-comparison/input-execution-v1/RESUME_20260920.md)에서 갱신하며, 명령 시작은 완료나 후보 채택을 뜻하지 않는다. 아래9GiB 실패는 앞선 이력이다.

## 2026-09-20 이전 관측 — S5 복구 동결·캐시 정리 완료, RAM 시작 조건 미충족

[재개 기록](content/model-comparison/input-execution-v1/RESUME_20260920.md)에 기존 9개 실제 결과 재검증, runner 합성 검사 17개·채점 컨트롤러 합성 검사 8개 통과, 새 v5 동결 9파일·기존 동결 43파일 대조를 기록했다. 실제 사전검사는 300.781초·63회 관측 동안 가용 RAM 6.860~7.344GiB로 9GiB 기준에 미달해 종료됐다. 사용자의 정리 지시로 재생성 가능한 webpack/Python 캐시 76파일·59.83MiB를 개별 삭제했으며 정리 후 RAM도 7.228·7.250GiB였다. 종료할 저장소 소유 잔류 프로세스는 없었다. 새 모델 호출·run/claim·채점·앱 변경은 0회이며 질문 9/64개와 기존 앱 등록을 유지한다. 앱 build/E2E·실제 HTML/PDF 번역 검증은 이번에 실행하지 않았다. 여유 physical/commit 각각 9GiB를 확보한 뒤 남은 55개 생성부터 이어간다.

## 2026-09-14 — S4 실제64개 완료·S5 질문9개 보존과 나머지55개 복구 준비

최신 사용자 지시로 S4부터 조건부 S6까지 진행한다. 사용자는 2026-09-14 후속 지시로 충전기 없이 배터리 전원에서 계속 실행하도록 명시했다. [배터리 실행 v4](content/model-comparison/input-execution-v1/LOCAL_QUESTION_BATTERY_EXECUTION_V4.md)는 AC0/1과 알려진 배터리 잔량20% 초과를 허용하며 CPU4·메모리·소유 프로세스·시간 제한과 질문/평가 기준을 유지한다. 이전 AC 전용 실패·동결을 보존하고 새 실행 정체성으로 진행한다. 2026-09-14 17:10 KST에 배터리 실행 v4를 실제 시작했다. [39개 구현 검사와 동결 검증](.training/verifications/question-battery-freeze-verification-20260914.json)을 마쳤고 [실제 사전검사](.training/verifications/question-battery-preflight-20260914.json)는 배터리80%·AC0에서 통과했다. 첫 소유 native 프로세스의 입력/토큰 대조 후 질문 요청을 전달했다. 전체64개 답변 저장·종료·무결성 확인과 답변 동결 뒤 채점한다. 아직 완료나 후보 선택을 선언하지 않는다. 아래 AC0 중단은 그 승인 이전 이력이다. [새 실행 도구와 평가 안내](content/model-comparison/input-execution-v1/README.md)에 별도 producer·자원 guard·원시 HTTP 기록·원문 검토/독립 질문/후보 선택 계약을 연결했다. S1/S2/S3 동결 파일·활성 등록·운영 DB는 유지한다.

S4 `attempt-001`은 실제64개 template/token 대조 후38개를 저장했고39번째 요청에서 응답 없이 전원 감시 중단을 기록했다. 원래 failed 상태·원시 응답·소유 종료·최종 무결성을 보존했다. [복구 계약](content/model-comparison/input-execution-v1/RECOVERY_V1.md)의 `s4-recovery/recovery-attempt-001`은 새 두 차례 AC1/자원 사전검사와64개 서버 입력 대조 후26개를 모두 저장했다. elapsed3,072.766초, 누락0, 소유 native 종료·임시 전원 요청 해제·입력/출력 무결성 통과다. 실행기12개·복구 검증18개 검사를 통과해 동결한6파일은 유지한다.

임시 포트와 그에 대응하는 CORS origin 비교는 [별도 v2 검증 계약](content/model-comparison/input-execution-v1/RECOVERY_COHORT_VALIDATION_V2.md)으로 보완했다. 실제64개 논리 묶음 `s4-cohort-v2-20260914`와 정식 S5 packet `s5-formal-v2-20260914`를 만들고 재검증했다. 원래38개/새26개의 run ID와65회 요청 중1회 미확보를 그대로 기록한다. v2 코드 검사들은 서로 별도 실행으로 cohort17·평가/transport24·잠정/lineage13·관계7·원장13개가 통과했고 [13파일 동결](content/model-comparison/input-execution-v1/recovery-evaluation-freeze-v2.json)에 연결했다.

새 질문 에이전트 생성 한도로 [Qwen 로컬 독립 질문 경로](content/model-comparison/input-execution-v1/LOCAL_QUESTION_REVIEW_PROTOCOL_V1.md)를 구현·동결했다. 고정 설치의 runtime51개+모델1개를 정확히 구분하는 별도 교정9파일은 runner13·transport17·평가24·원장17개 검사를 각각 통과했다. 실제 시도는 claim 작성 후 PowerRequest 진입에서 중단되어 native/질문 호출0이다. 후속 두 관측에서 AC0/ac_power_required를 확인했고, [실제 중단 증거와 별도 재개 조건](content/model-comparison/input-execution-v1/QUESTION_POWER_STOP_20260914.md)을 남겼다. 원래 실패 summary의 상세 원인은 ResourceGuardError까지만 남아 있어 후속 전원 관측과 구분한다. 후속 [0회 호출 복구 v3](content/model-comparison/input-execution-v1/LOCAL_QUESTION_ZERO_CALL_RECOVERY_V3.md)는 기존3파일 실패/claim을 보존하고 새9파일을 [동결](content/model-comparison/input-execution-v1/local-question-zero-call-recovery-freeze-v3.json)했다. 전원·자원 사전조건 실패는 별도 audit에 남기며 새 run/claim을 소비하지 않는다. [실제 v3 사전검사](.training/verifications/question-recovery-preflight-20260914.json)에서 기존 설치 전체 해시·Python·모델 metadata를 검증한 뒤 두 관측 모두 AC0/ac_power_required를 확인했다. 당시 v3의 native·질문 호출·새 run/claim은0이었다. 이후 v4 실제 실행을 시작했으며 채점은 전체 답변 동결을 기다린다. 한국어 packet마다 완전히 새 native 프로세스에서 한 번 답변하며 실제 생성·해시·격리·종료를 확인한 뒤 전체64개 답변을 먼저 동결한다. 도구의 합성 검사와 실제 QA 완료를 구분하며 S5 질문·채점·후보 선택과 S6은 아직 미완료다. 이번 작업에서 앱 build/E2E·HTML3문단/PDF1페이지 앱 생성/저장/캐시 검증은 실행하지 않았고 운영 DB·활성 등록·기존 번역 검토146개는 유지했다.

## 2026-09-13 — S2 입력 준비·S3 저장 출력 진단 완료 당시 기록

[S2/S3 완료 기록](content/model-comparison/input-preparation-v1-s2s3/README.md)에 새 문맥·출현별 뜻 선택기·비교 입력 연결·어휘 전용 native tokenizer·관계 검사기를 정리했다. 운영 경로와 별도 Python 코드이며 S1 동결 자료·등록 사전/코드·가중치·개인 DB·원문 버전은 보존했다. Windows 코드 검사78개(문맥22·사전24·통합13·관계v1 6/v2 8·원장5)가 통과했다. 앱 build/E2E는 실행하지 않았다.

S2는16단위×C0~C3의64개 입력을 `s2-prepared/attempt-002/`에 확정했다. 실제 native tokenizer88요청·전체 prompt281~621토큰,4095상한 내이며 C0의 실제 Node 문맥 식16/16과 등록 builder/Jinja를 대조했다. 성공 실행11.750초·최대 working set103,772,160바이트·어휘 로드1·context/가중치 tensor/생성0·소유 종료 exit0이다. 앞선 smoke와 저장 직전 경로 처리 오류가 난 attempt-001도 보존했고, 해당 기술 오류 수정 후 새 attempt에서 완료했다. 새 사전은 이번16단위에서 금융 힌트0개이며 C2/C3의 의미 분별 효과가 실증된 것은 아니다. 사전 변경이 실제 prompt에 영향을 준 대상은 기존3힌트를 제거한 F02/F04뿐이다.

S3는 기존98출력·122판정과930근거 파일 해시를 보존한 진단이다. 합성64반례가 통과했고 실제 경고는 기존 TG27 상대%→%p 오류1건 재검출이다. 일부 관계가 지원된 출력36/98, 관계 상태40/588이며 미지원·보류는 통과가 아니다. 초기v1 표기 오탐5와 결과를 보존한 뒤 새v2에서 보완했다. 원장에는 relation_observation98개만 추가하여738→836사건, 반복 추가0/재사용98, 번역 검토146개 그대로다. 관계별 독립gold·QE95/95·생성 품질 개선·독립 holdout·앱 적용은 미완료다.

S4 읽기 전용 점검은2026-09-13 07:31 UTC 기준 ACLineStatus=0, 가용 physical11.535GiB/commit19.850GiB였다. native 이름 일치0·명시worker0이지만 미분류Node가 있어 모든 실행기 부재를 확정하지 않았다. 새 생성기·상호 배제·실행 중 자원 감시 구현과 Hy7 전용 자원 조건 동결, 실제 AC/자원 재확인·서버 endpoint 대조가 남아 있다. 이 점검으로 모델을 시작하지 않았다. S4~S6와 후순위 TG27 읽기6은 미완료다.

## 2026-09-13 — 공통 입력 준비 S1 완료 당시 기록

[S1 완료 자료](content/model-comparison/input-preparation-v1/README.md)에 현재 Hy7 입력 감사·입력/평가 계약·별도 사전과 자료를 고정했다. 개발16단위(금융8/일반8)는 실제 공개 회귀4·새 합성12이며, 핵심 명제48·질문32(핵심16)·선정 용어39출현이다. 사전8개념·24뜻·11출처, C0~C3의 정책/공통 생성 조건·4095입력토큰 한도·완료/중단·정답 격리, 후속 독립 금융60/일반20의 선정 절차를 먼저 정했다.

Windows에서 새 S1 검증기로 JSON·묶음 내부 소속·인용·용어 위치·분모·원문/자료 해시를 확인했다. 별도 읽기 전용 DB 대조로 공개 원문 snapshot의 실제 소속·텍스트·구조를 확인했다. 현재 등록·모델 전체·runtime/code 등104파일 및 기존14데이터 파일의2,345원문/문맥 행을 대조했고 소비 test와의 중복0·새 그룹 밖 중복0이었다. 공개4대상은 모두 Marian train과 겹쳐 회귀로 명시했고 공개 본문과 평가 인용은 Git 제외 로컬 source-bundles에 보존했다. 별도 도우미 원문/계약 검토에서 발견한 부정·가정·상대%와 질문 격리 문제를 동결 전에 보완했다.

이 결과는 입력·평가 자료 검증이다. **S2~S6 미착수, 모델/실제 tokenizer 호출0, 앱 build/E2E 미실행**이다. 새 선택기·관계 검사기의 구현 완료나 생성/탐지 품질 향상을 주장하지 않는다. 운영 DB·개인 기록·원문 버전·등록·기존 원장은 변경하지 않았다. 현재 전원/RAM과 향후 독립 답변자 확보는 이번에 확인하지 않았으며 실행 전에 별도 확인한다.

## 2026-09-13 — S1 착수 전 계획 기록

최신 작업 순서는 [인수인계28절](TRANSLATION_HANDOFF_20260911.md#28-공통-입력-처리와-의미-관계-검사-개선-실행-계획)이다. 구조 문맥·문맥별 의미 사전·관계 검사 준비를 우선하고, 동일 Hy7의 현재/문맥/사전/결합 네 구성 비교·일반 회귀·독립 평가 이후 통과한 구성만 앱 검증으로 진행한다. TG27 읽기6·57용어·독립 질문은 후순위 미완료로 보존한다. 아래27절의 프로세스 부재·RAM·사용량 제한은2026-09-12 당시 관측이며 이번에 재측정하지 않았다.

이번 변경은 인수인계·요구사항·시스템 설계·README·구현 상태의 문서 정리다. S1~S6은 아직 미착수이며 앱/도구 구현·실제 추론·가중치 학습·등록·운영 DB 변경은 없다. 검증 범위는 문서의 링크/경로·계획 정합성·이번 diff이며 앱 테스트나 새 번역 엔진 검증을 수행한 것으로 보고하지 않는다.

실제 문서 검사에서 인수인계2~27절 보존, 새 로컬 링크/목차 연결20개와 단계6개의 미착수를 확인했다. 별도 도우미 검토에서도 중요한 계획 모순·완료 오표시가 없음을 확인했다.

## 2026-09-11 후속 — 의미 위험 표시 구현, 학습 수용 기준 별도 확정

**최신 상태: 2026-09-12 개발 원문18개 대조 완료, 읽기6 재시도는 사전검사 보류다.** 고아 Next telemetry1개와 재생성 가능한 캐시149파일·약189MiB를 정리했다. 원문·DB·개인 기록·모델·체크포인트는 보존한다. 최신 실행 상태는 [인수인계27절](TRANSLATION_HANDOFF_20260911.md#27-읽기6-중단-증거와-재시도-사전검사-보류)을 따른다. 아래 시각별 관측은 과거 이력이며 현재 단일 개발18 성공을 뜻하지 않는다.

13:30 KST 후속: 새017·018은13:22:55 KST에 종료·해제됐고 고정2개 기술 검증을 통과했다.017의 인과 입증 부정은 보류,018의 감사/평가 업무·잠정 추정 지시 관계는 보존으로 판정했다. 새 번역2·용어11 사건 추가, 반복 추가0·재사용13을 확인해 원장은737사건이다. 기존 실패16과 새2의 원문 중복/누락·용어92출현·근거342파일을 대조한 진단은 중요5·경미2·관측 오류 없음5·보류6, 용어81보존·4오역·7보류다. 전체 개발18 단일 실행 검증은 false로 유지한다.13:24 KST부터 읽기6의 첫 요청을 처리 중이며 독립 질문 평가·전체 수용·앱 적용은 아직 완료하지 않았다. 이번 추가 검증은 CLI 증거 연결과 실제 로컬 생성이며 앱 전체 테스트가 아니다.

11:13:57 KST에 기존 TG27 실행은17번의7200초 제한으로 종료됐다. 완료16·실패1·미실행1, 소유 종료·전원 요청 해제와58파일을 확인했다. 17번의 긴 관측 공백 합5415.046초와 그 사이 native CPU 증가14.28125초를 구분한다. 실행 실패1건만 원장에 추가해611사건이 됐고 중복 재입력은 추가0·재사용1이었다. 완료16개의 의미/용어 판단은 그대로 보존했다.

[별도 재실행 계약](content/model-comparison/TG27_TAIL_RECOVERY_20260912.md)에 따라11:19 KST 새 사전검사 후 기존 v5 producer의 `--ids LDEV26-017 LDEV26-018`를 시작했다. CPU4·BelowNormal·8GiB·기존 시간 제한을 유지하며, 두 문단의 종료·별도 검증·새 자원 검사 뒤 읽기6을 실행한다. AC 전원에서 일시적 display+system 요청을 유지하고 종료/전원 분리/취소 시 해제한다. 전역 전원 설정은 변경하지 않는다. 고정2개 증거 검사 합성6개와 새 부분 보존 계약11개를 통과했으며, 새 실제2개/읽기6 완료·질문 평가·앱 적용은 아직 미완료다. 기존 전체18 검증기가 실패 실행과2개 실행을 거부하는 기준은 유지한다.

11:37 KST에는 [실패 실행 부분 진단 연결](scripts/model-comparison/PARTIAL_TG27_REVIEW.md#실패-실행의-완료16개를-진단-원장에-연결)을 실제 수행했다. 고정 실패 증거와16개 snapshot을 재검증해 번역16·용어81 판정을 추가했으며 원장은708사건이다. 새 합성6개 통과, 실제97건 추가·반복 추가0/재사용97을 확인했다. 모든 사건에 실패 실행·부분 검증·비맹검 이력과 원래 자동 검사를 보존하며 전체18 성공·전체149 정확도·학습 수용을 주장하지 않는다. 아래의 미편입 문구는 그 이전 관측이다.

새2개/읽기6에 사용할 [완료 검토 연결](scripts/model-comparison/PARTIAL_TG27_REVIEW.md#새2개와-읽기6의-완료-검토-연결)은 별도로 준비해 합성6개를 통과했고, 실제 미완료 두 실행의 거부·output/원장/native0을 확인했다. 현재 새 결과의 의미·용어 편입은 아직 없으며 기존708사건을 유지한다. 기술 검증 도구 준비와 실제 엔진의 전체 검증을 구분한다.

11:54 KST에 기존 숫자 검사 및 별도 의미 규칙을 TG27 실제 부분 결과에 적용해 검사 범위를 확인했다. 숫자 함수는2%/2%p를 같은 토큰으로 읽고, 기존 의미 규칙은 중요5 중006 한 문단만 경고했다(TP1·FP0·FN4·TN6·보류5).16개 관측을 추가해 현재724사건이며 중복 추가0·재사용16이다. [근거와 한계](content/model-comparison/ERROR_LEDGER_REPORT_20260911.md#기존-검사-코드에서-확인한-범위)를 기록했고 원래 코드/번역/자동 검사는 변경하지 않았다. 이 검사 함수의 실제 출력 확인을 앱 전체 테스트나95% 수용으로 보고하지 않는다.

12:21 KST까지 새017도112토큰·EOS106으로 생성됐고 원문 대조를 새 부분 폴더에 보존했다. 통제변수·추정 상관·유의성 한계는 유지됐으나 인과 입증의 부정이 충분히 명확한지는 보류했다. 현재 서로 다른2개 실행의17원문은 중요5·경미2·관측 오류 없음4·보류6, 용어83출현은72보존·4오역·7보류다. snapshot102파일과 기존 판정 근거를 확인했다.018 생성 중이며 읽기6·독립 질문·전체 기술 완료는 남아 있다. 새017은 원장에 아직 편입하지 않았고 기존724사건을 유지한다.

TG27 첫 재개는1개 생성 후 둘째 문단의 긴 관측 공백과7200초 요청 제한으로 종료됐다. Windows 절전 이벤트·부분 원시 응답·소유 종료·전원 요청 해제 등79파일을 확인했으며 전체18개 비교 완료나 의미 품질 실패로 바꾸지 않는다. 사용자의 연결 실패 재시도 지시 후9GiB는 실제 로딩 직전 RAM 조건 때문에 두 번 거부됐고 native 로딩0회였다. 실제 producer 관측과 새 두 관측으로8GiB를 선택해 `translategemma-27b-v5-user-retry-20260911-b8`를 시작했다. CPU4·BelowNormal·코드35개 snapshot과 단일 native를 유지한다. 이 새 실행의18개와 읽기6은 아직 미완료다.

00:45 KST까지 완료된 첫2문단은 [부분 대조](scripts/model-comparison/PARTIAL_TG27_REVIEW.md)로 의미 관계8개·용어14출현을 직접 검토해 원시 응답·원문·판정·코드 해시와 함께 보존했다. 지급 의무자/수취인을 바꾼 첫 대조쌍의 뜻은 유지했다. 통화 기호 검사 실패는 `$`→`달러` 표기 변화로 그대로 기록하며 별도의 의미 판단과 구분한다. 부분 보존 도구의 합성9개와 실제2건을 확인했으나 전체 실행·독립 한국어 답변·학습 수용을 통과한 것은 아니다.

01:17 KST 세 번째 문단까지의 부분 대조는3문단·용어24출현 중21개 보존·3개 보류였다. 세 번째는 자동검사를 통과했지만 `lender`의 당사자 명확성과 `waiver`2출현의 범위 한정을 보류했다. 당시 기록은 보존하며 다음 최신 대조 결과와 구분한다.

**08:41 KST 관측·첫15문단 대조 완료:** [실제 부분 검토 보고서](content/model-comparison/TG27_PARTIAL_REVIEW_20260912.md)에 중요 의미 오류5문단·경미2·관측 오류 없음4·보류4를 기록했다. 용어76출현 중66보존·4오역·6보류이며 전체149출현의 정확도는 아니다. 상대2%→2%p, 금융 equity→형평성, 통합 이후→통합 과정, overhead→불필요한 비용 등 실제 자동 검사 통과 문단의 의미 손상을 확인했다. 새004~015 판단을 검증한 뒤 별도 보존했고 snapshot90파일·receipt15개 해시/정체성을 확인했다. 16번째 생성과 순차 coordinator는 계속 진행 중이며 전체 v5 종료·읽기6·독립 질문 평가가 남아 있다. 중요 의미 오류0 기준을 만족한 것으로 처리하지 않는다. 원문24/질문48/핵심24와 질문지 출처·정책 해시는 별도 확인했지만, 독립 답변 도우미는 서비스 사용량 제한으로 사용할 수 없어 답변·동결·채점이 아직 없다. 원문을 본 root의 답변으로 대체하지 않았다.

새 [완료 대조 adapter](scripts/model-comparison/PARTIAL_TG27_REVIEW.md#전체-실행-종료-후-연결)는 기존 v5 완료 증거와 집합18/6의 모든 부분 수동 판정을 연결하도록 준비했다. 새6개+기존9개, 합성15개 통과 및 실제 미완료 실행 거부(완료 요약 없음)를 확인했다. 보류·비맹검 이력·원래 자동 검사와 고정 용어 분모를 유지하며, 현재 전체 산출물·원장 추가·native 호출은0이다. 완료 결과 편입 성공과 앱 테스트 통과를 새로 주장하지 않는다.

후속016도 생성·원문 대조·별도 보존했다. 현재16문단은 중요5·경미2·관측 오류 없음4·보류5, 용어81출현은70보존·4오역·7보류다. 016의 수식·금액·운송비 이중 차감 금지는 유지했으나 sale proceeds→판매 수익은 범위를 보류했다. snapshot96파일 확인을 새 기록에 보존했다. 017은 유휴 Modern Standby와 겹친 긴 기록 공백이 반복되어 요청 시간이 늘었다. 11:02 KST에 전원 연결을 확인하고 기존 coordinator의 handle/생성시각/실행 경로에 묶인 일시적 display+system 전원 요청을 추가했다.2초 실측에서 요청 설정·해제·handle 닫기를 확인했고, 실제 도우미는 종료/취소/AC 분리/24시간 상한에 해제한다. 전역 전원 설정·모델/worker·기존 실행 코드·시간 제한은 변경하지 않았다. 전체 생성·검증 성공은 아직 미완료다.

재개 후 Qwen v5 준비 연결의 설치 manifest 누락을 수정하고 합성89개·실제 vocab8개·준비 해시/종료 검증을 완료했다. 실제 의미 전용 진단은 첫 LDEV26-010의 수량/정체성 오류를 놓쳐1/8에서 사전 규칙대로 중단했다. 원시 응답·guard·소유 종료를 검증했으며 나머지7개·전체48·등록은 하지 않는다. [v5 보고서](content/model-comparison/QWEN35_V5_REVIEW_20260911.md)에 기술 통과와 품질 실패를 구분한다.

[통합 오류 원장](scripts/model-comparison/error-ledger/README.md)은 합성12개와 실제610사건 연결을 확인했다. 번역 검토128·질문76·QE104·실행4·용어298을 구분하고 같은 출력의 후속 검토를 합산하지 않는다. [원인별 분석](content/model-comparison/ERROR_LEDGER_REPORT_20260911.md)은 관측과 미확정 원인을 분리한다. 원문24개·용어149출현·제외44개도 전수 root 검토했고 기간/주기 주석1개만 별도로 정정했다. [후보별 용어 채점](content/model-comparison/FINANCE_TERM_REVIEW_20260911.md)은 Hy7 전체141/149, Hy30 전체139/149(보류3 포함)로 모두95%에 미달했다. 비맹검 도우미 판정이며 문단 의미/질문과 구분한다. 이번 후속은 운영 DB·앱 기능을 바꾸지 않았으므로 아래 앱 검사 결과를 이번 실행으로 보고하지 않는다.

사용자의 전체 진행 승인 뒤 자동 의미 검사 이력·비동기 작업·캐시·reader 표시와 모델 수명 관리를 구현했다. migration 003의 별도 평가 테이블을 사용하고 원래 기계 번역·숫자 검사·사용자 검수를 분리한다. 유효한 중요 오류 구간만 빨간 물결 밑줄, 위치를 특정하지 못한 저신뢰 결과는 주황 문단 표시를 사용한다. 영어 문장 전체·근거 보기·번역 수정으로 연결한다. 검수본을 우선하고 늦은 폴링 응답이나 평가 결과로 되돌리지 않는다. QE 미설치/미등록/실패에서도 이미 확인한 좁은 규칙 경고는 보존한다.

번역 프로세스는 활성 요청이 없는 유휴 60초에 반환하며 모델 전환·worker 종료에서 실제 소유 프로세스 종료를 기다린다. 평가 종료 실패 뒤 새 번역 모델을 겹쳐 로드하지 않도록 차단했다. 취소 중 lease 복구의 평가 상태 정체와 새 작업 그룹의 unique 충돌을 보완하고, 복원 후 대기 평가도 취소해 자동 재개하지 않는다.

최종 단위 검사 173개 중 171개 통과·실제 PDF 조건부 2개 제외(74.909초), production build·TypeScript를 통과했다. 초기165개 실행과 이후 취소/그룹 경합·예약 실패 경고 정리·현재 제품 정책만 허용하는 검증을 구분하며 최종 로그는 `test-results/quality-full-unit-final-20260911.log`, `quality-build-complete-20260911.log`다. reader 모의 API E2E 9개와 1440/390px 화면 검증을 통과했으며 모의 검사에서 모델 호출은 0회였다.

실제 Hy7 엔진은 별도 격리 schema3 DB에서 HTML 3문단과 PDF 페이지번호3의 전체4블록, 총7개를 생성·저장했다. 920.062초, 기존 자동검사7/7, 번역·의미 검사 캐시 재사용 시 추가 작업/사용량0, 원문·블록 불변, 소유 프로세스 종료를 확인했다. 미등록 QE는 score=null·unavailable로 남는다. 실행 코드7개와 [실제 앱 보고서](.training/quality-evaluation/app-hy7-quality-20260911/derived/quality-app-report.json)를 보존한다. 별도 [비맹검 도우미 의미 검토 v1](.training/quality-evaluation/app-hy7-quality-20260911/derived/quality-app-assistant-meaning-review.json)는 본문5·제목1·쪽번호1을 구분해7/7 대조하고 수혜주체 해석1건을 보류했다. [후속 조정 v2](.training/quality-evaluation/app-hy7-quality-20260911/derived/quality-app-assistant-beneficiary-adjudication-v2.json)는 이웃 문맥의 과거 투자·미래 창출력 대비가 보존되는 근거로 이를 경미한 지시 대상 한정으로 확정했다. 선택 표본의 최종 관측은 critical0/major0/minor4, 미판정0이다. 사람 검수나 독립 블라인드 평가는 아니며 **질문·용어·새 시험 등 전체 학습 수용은 여전히 미충족**이다. 최초 원문 체크포인트의 한국어 인코딩 손상과 번역 열람 후 별도 복구본도 기록했다. 원래 판정·번역·운영 검수 이력은 변경하지 않았다.

운영 DB는 `data/backups/2026-09-11T02-07-21-790Z-ad748cd6`의 SQLite API·참조파일53개 일관 백업 후 schema2→3을 적용했다. 메모·북마크·진도·설정 우선순위·번역·검수·사용량·작업·원문 등14개 테이블의 전후 행 해시·개수가 모두 같고 FK 검사0·integrity=ok다. 새 평가 이력0이며 worker/유료 호출을 시작하지 않았다. 근거는 `.training/verifications/schema3-before-20260911.json`, `schema3-after-20260911.json`이다.

독립 wmt20 COMET-QE CPU 후보는 48/48 점수화했지만 기존 기준과 새 95/95 기준 모두 실패해 **미등록**이다. 기지 핵심 6개를 모두 경고하려면 45/48에 경고해야 했고 중요 오류 정밀도는 14/45=31.11%였다. 최초 로드 호환 실패, 후속 141.516초 실측·peak 4.89GiB, 원시 점수·코드·교정·등록 거부를 [QE 안내](scripts/local-qe/README.md)에 구분했다. 고정 규칙은 기존 48개에서 중요 오류 14개 중 나눗셈 반전 1개를 탐지하고 오탐 0개였으며 광범위 의미 탐지기로 통과한 결과가 아니다.

학습용 수용 기준은 [별도 정책](content/model-comparison/LEARNING_READINESS_BASELINE_20260911.md)으로 확정한다. 각 평가 집합에서 critical/major 의미 오류 0건, 기타 용어·질문·위험 경고 지표 95% 이상과 핵심 질문·기지 오류·기술 offset의 별도 기준을 적용한다. 보편적인 학술 안전선이나 인간 학습 효과 검증으로 표시하지 않는다. 기존 85/60 QE 실험 기준·판정은 변경하지 않고 향후 등록 기준만 새 버전으로 적용한다.

CPU·메모리 최적화 후속 실험은 모델 하나씩 CPU4/BelowNormal로 진행한다. Hy7 raw/contextual 일반16을 각각 생성·무결성·종료16/16 완료했다. 원문 전용은 major3·보류1, 질문30/32·핵심15/16이고 문맥 프로필은 major2·보류1, 질문29/32·핵심14/16으로 모두 기준 미달이다. 유창성16/16을 의미 통과로 바꾸지 않으며 [일반 문맥 비교 보고서](content/model-comparison/GENERAL_CONTEXT_REPORT_20260911.md)에 새 한국어 전용 답변자·원문 명제 검토·채점·답변자 차이를 구분했다. 두 실행의 peak WS는 약7.94GiB이며 문맥 프로필2917.860초에는 Modern Standby 공백이 포함된다.

Qwen 검사 후보 v4의3개 기술 검사에서1058토큰씩2회 캐시 복원을 확인하고 요청 시간 합계를504.375→427.125초로 줄였지만 peak WS는20.67MiB 늘었다. 이후 전체48은12개 생성 후 절전과 겹친 공백으로 요청guard가 발동했다. 소유 종료·파일 무결성과 완료12개를 재검증했으나 그중 중요 오류3개를 모두 놓쳤다. 나머지를 모두 맞혀도재현율상한11/14=78.57%라36개 추가 추론을 생략한다. [Qwen 실패 보고서](content/model-comparison/QWEN35_V4_REVIEW_20260911.md)에 원시·기술·품질·상한·절전 증거를 구분했으며 앱에 등록하지 않았다. TG27의 새 v5 CPU/메모리 관측 실행기와 검수 연결은 합성53개/65개를 통과했고 실제18+6 실행이 남아 있다. [최신 인수인계19절](TRANSLATION_HANDOFF_20260911.md#19-qwen-완료12개에서-기준-미달-확정tg27-v5-준비)을 따른다.

후속 자원 검사 두 번의 보류 뒤 17:11 KST에 가용 RAM 최소11.06GiB를 확인해 TG27 v5 개발18을 CPU4/BelowNormal·8GiB 상한으로 시작했다. 첫 문단에서 실제 소유 native의7.94GiB 작업 집합·우선순위·CPU 증가를 관측했고 실행 코드35개를 보존했다. 완료18·읽기6·새 의미 검수는 아직 남아 있다. Qwen 새 의미 전용 계약·후처리와 첫 오류에서 중단하는 개발8·6GiB 프로필은 별도로 준비 중이며 동시에 모델을 실행하지 않는다. 자세한 진행은 [인수인계20절 이후](TRANSLATION_HANDOFF_20260911.md#20-ram-시작-조건-보류금융-질문지-동결)를 따른다.

금융 질문 준비/답변 동결/수동 판정 도구는 합성13개를 통과했다. 저장된 Hy30 읽기6의 실제 증거를 다시 검증하고 새 한국어 전용 답변자를 통해12문항을 평가한 결과 **10/12·핵심4/6**으로 기준 미달이다. 답변을 먼저 동결하고 원문 근거로 별도 채점했으며, 질문이 과거 출력보다 나중인 사후 개발 평가임을 [읽기 질문 보고서](content/model-comparison/FINANCE_READING_ANSWERABILITY_REPORT_20260911.md)에 명시했다. 새 번역 엔진 호출은0이고 이 질문 결과로 기존 문단 의미 판정을 덮어쓰지 않았다.

P0 앱·데이터 저장·원문 리더·번역 처리 코드가 구현되어 있으며 실제 원문과 개인 기록 흐름을 확인했다. 초기 요구에 따라 **무료 Argos 로컬 번역을 기본으로 전환하고 설치했다.** 후속 Hy-MT2 선택과 운영 상태는 아래에 구분한다. API 키 없이 실제 R01 원문 3문단의 한국어 생성·DB 저장·캐시 재사용을 확인했다. 번역은 미검수 초안이며 전문 번역 품질을 보장하지 않는다.

초기 NYU 대표 PDF 다운로드는 시간초과로 실패했으나, 후속 NAT64 주소 처리 수정 뒤 R06의 실제 PDF 원본 다운로드와 PDF.js 10페이지 추출을 확인했다. 운영 DB 밖 검증 폴더에 보존했으며 실제 PDF를 사용한 새 제공자의 번역·저장·캐시 확인은 후속 단계다. 사용자는 무료 로컬 모델만 사용하기로 확정했고 OpenAI 실호출은 수행하지 않았다.

원문 UI에서 외부 수집과 로컬 다운로드를 분리했다. 자료·Excel 상세에서는 `원저자 사이트 → 앱 보관함`과 `앱 보관함 → 내 파일`을 별도 영역·아이콘·경로·`인터넷 필요/불필요` 배지로 표시한다. Excel 원본 영역은 모바일에서도 가이드보다 먼저 보이도록 본문 상단으로 옮겼고, 보관본이 없으면 `원본 가져오기`, 있으면 `보관본 다운로드`를 먼저 보여준다. 카드 제목·설명도 12~14px로 키웠으며 진행 상태는 import 작업만 판정해 번역 작업을 가져오기 중으로 오인하지 않는다. `npm run typecheck`, `npm run build`, `npm test` 93개(통과 91·실제 PDF 조건부 제외 2), 격리된 `npm run test:browser -- --provider=argos` 19개가 통과했다. 브라우저 검증은 1440px와 390px에서 영역 순서·구분 문구·고유 접근성 이름·링크 대상·download 속성·가로 넘침을 확인했고 `test-results/e2e-source-actions-desktop.png`, `test-results/e2e-source-actions-mobile.png`에 화면을 남겼다. 격리 DB의 실제 Argos 번역 회귀 검증과 UI 검증을 포함하며 운영 원문·개인 기록은 변경하지 않았다.

## 후속 언어학적 비교 — 7B·12B 검토 완료, 대형 후보 실행 확인 중

처리 속도보다 의미 정확성과 한국어 품질을 우선하는 새 비교를 진행했다. Hy-MT2 7B Q8 contextual과 TranslateGemma 12B Q4 원문 전용 구성을 각각 새 개발 자료 18개와 공개 원문 6문단에서 실제 실행하고, 모델 이름을 가린 48개 판단과 경계 사례의 추가 대조를 완료했다. 중요한 의미 오류는 개발 자료에서 4/18 대 6/18, 공개 원문에서 1/6 대 3/6이다. 12B로 교체할 근거가 부족하여 기존 등록을 유지한다. 7B도 금융 문맥의 equity와 단기 국채 등을 틀렸고, 주석된 용어 12개 출현의 12/12를 금융 용어 전체 90%나 의미 정확도로 해석하지 않는다. 사람 검수는 없으며 새 독립 최종 시험이 아니다. [후속 비교 기록](content/model-comparison/LINGUISTIC_COMPARISON_REPORT.md)에 원시 실행·사전 검수 조정·집계 해시와 한계를 기록했다.

완료한 실행의 모델 프로세스를 종료한 뒤 다음 후보를 순차 실행한다. 27B·30B 모델 파일 설치·전체 해시 검증과 별도 메모리 제한 실행기 준비를 마쳤다. 27B의 첫 8GiB 상주량 제한 기능 시험은 2026-09-10 13:03:42 UTC에 일반 입력 2/2건으로 완료됐으며 무결성·메모리 감시·소유 자식 종료 증거를 확인했다. 이어 실행한 30B v3의 top_k 보고값 검증 실패는 보존했다. 같은 비활성 동작을 명시적으로 검증하는 v4의 기능 입력 2/2건에서 원래 EOS·출력 무결성·종료를 확인했고, 별도 30B 개발 18행도 생성·출력 무결성·모델 종료 확인을 완료했다. 의미 검수와 공개 원문 6문단 실행은 아직 남아 있다. 초기 11GiB 계획은 27B에서 관측된 낮은 시스템 물리 여유를 고려해 8GiB로 낮췄으며 샘플링·품질 기준은 유지했다. 27B v4는 요청 2시간·총 24시간으로 시간 한도만 늘렸다. 대형 후보의 생성 완료는 금융 의미 품질 검수 완료가 아니다. 시작 전 물리 여유·commit 검사, 소유 프로세스의 중지 상태 생성·한도 적용, 주기 및 요청 전후·마지막 메모리 검사, 실패·종료 확인을 구분한다. 이 변경은 별도 비교 CLI이며 운영 DB·등록 manifest·학습 가중치를 수정하지 않았다. 학습용 번역의 [최소 수용 정책](content/model-comparison/TRAINING_QUALITY_FLOOR.md)은 문서이며 자동 의미 검수 기능 구현 완료를 뜻하지 않는다. 사용자의 세션 인계 요청으로 다음 모델 실행을 시작하지 않고 보조 프로세스를 해제했으며, 현재 상태·설계 감사·후속 명령은 [인계 문서](TRANSLATION_HANDOFF_20260911.md)에 정리했다.

## 무료 로컬 번역 전체 품질 — 비교·재등록 완료, v2 앱 기능 검증·Hymt 활성화·운영 HTML 확인 완료

사용자는 무료 로컬 모델만으로 현재 PC에서 가능한 전체 번역 품질을 최대한 개선하고 싶다고 확정했다. 용어 90%는 중간 목표로 유지하며, 새 긴 문단 24개(금융 16·일반 8, 참고 문맥 8)를 독립 작성·전수 대조·누수 검사 후 확정했다. Argos 앱 경로·Marian v5 FP32·Hy-MT2 Q8 원문만·문맥 사용의 실제 번역 96개와 익명 도우미 판단 96개를 완료했다. 의미 오류 문단은 각각 24/24·20/24·10/24·5/24였으며 **Hy-MT2 문맥 사용 구성**을 선택·등록했다. 최초 등록 ID는 `hymt-contextual-q26-20260910`, manifest SHA는 `8f898e6358c336e6eef44a562b84da63e861d7deb327399959e2aabbc8e12840`이며 이후 실행기 수정 전의 기록으로 보존한다. 선택 구성에도 오류가 남고 사람 검수 자료는 없으며, 기존 v5 판정·가중치·원시 비교 결과는 보존했다. [로컬 전체 품질 비교 기록](content/model-comparison/QUALITY_LOCAL_REPORT.md)에 범위와 근거를 기록했다.

선택형 `hymt`의 원문·문맥 전달, 전체 등록 정체성 검사, 캐시·무료 사용량 분리와 기존 유료 작업 재개 차단을 구현했다. 관련 TypeScript 검사 32개와 타입 검사를 통과했다. 앱 실행기의 Windows Job 정리 13개·엔진 mock 26개·NDJSON mock 14개도 통과했다. 이 기능 검사와 별도로 실제 등록은 완료했지만, 등록 후 첫 앱 QA는 모델 시작 단계에서 실패했다. [실패 기록](test-results/hymt-runtime-verification-1789021931429.json)은 `real-translation` 단계·완료 검사 0개를 기록하며, 앞선 PDF 추출은 `pdf-paragraphs-v1`의 실제 3페이지 7블록 전문·순서까지 확인했다. 후속 [v1 앱 QA](test-results/hymt-runtime-verification-1789024231920.json)에서는 HTML 3문단·PDF 3페이지 7블록의 실제 생성·저장·캐시와 자동 검사를 모두 완료했다. PDF 도입부의 내용 추가는 후속 v2 원문 대조에서 해소됐다. 이 격리 검증 당시에는 운영 원문·개인 기록과 Argos 설정을 보존했으며, 이후 운영 제공자 선택은 아래에 별도로 기록한다.

이후 `process_owner.py`를 Windows `PROC_THREAD_ATTRIBUTE_JOB_LIST`로 native 서버 생성 시 전용 Job을 명시 배정하도록 수정했다. 확정 코드 SHA는 `9a00d809466e304359fb47c374c8fc2464ce31cdbc9557fbc9eee089bfe9d8b2`다. [통합 검사 67개](.training/verifications/hymt-job-list-tests-20260910-final.json)가 모두 통과했고, [실제 제품 엔진 시작·정상 종료](.training/verifications/hymt-native-job-startup-20260910.json)도 exit 0으로 확인했다. 이 시작 검증의 원문 전송·생성 요청은 0개이며 번역·저장·캐시 검증과 구분한다. 실제 등록 CLI로 새 ID `hymt-contextual-q26-20260910-nativejob`, manifest SHA `cb3031d87372fbb1b0c3dcaf2071064bb59a8a21bd96480ef2d5a66752ae52bd`를 등록했고 v1 앱 QA를 완료했다. 최초 등록 코드 6개의 동일 바이트는 [보존 명세](.training/verifications/hymt-initial-registration-code-20260910/manifest.json)에 남겼으며 최초 QA 실패 기록도 유지한다.

[HTML 도우미 대조](.training/verifications/hymt-app-html-assistant-review-20260910.json)에서는 3문단 모두 핵심 의미가 보존됐고 용어 정밀도 1건·문체 불일치 2건이 남았다. [PDF v1 도우미 대조](.training/verifications/hymt-app-pdf-v1-assistant-review-20260910.json)는 자동 검사 7/7 통과와 별개로, 미완결 도입 블록에 다음 하위 항목의 내용이 추가된 실질적 오류 1건을 확인했다. 본문의 `자산 in place` 영어 잔류 등 경미한 문제도 남았다. 원래 원문·출력·QA 기록을 보존하며, Q26 시험·가중치·프롬프트·사전은 바꾸지 않고 새 PDF 추출 버전으로 번역 단위를 보완한다. 이 기록은 도우미 대조이며 사용자 검수 완료로 바꾸지 않는다.

통합 후 `npm test`의 **67개 전체 앱 검사**가 통과했다(52.981초, skip 0). 실제 모델 호출을 포함하지 않는 기능·회귀 검사이며 테스트 DB는 운영 자료와 분리했다.

이후 NAT64 주소 처리 및 등록 당시 Python·Jinja 바이트/목록 검증을 반영한 `npm test` **73개가 모두 통과**했다(59.601초, skip 0). 등록 재검증 이후 원시 비교 입력·검토 JSONL의 동시 변경도 거부하도록 보완했다. 별도 관련 Python 검사 7개와 TypeScript 검사 5개가 통과했으며 실제 품질 결과나 등록을 만들어 내는 검사는 아니다.

추가 가상환경 Python 통합 검사에서는 총 68개 중 Windows 프로세스 검사 3개가 실패했고 symlink 권한 검사 1개는 제외됐다. venv launcher와 실제 Python의 PID·종료 관계를 아래와 같이 수정·재검증했다. 원문 다운로드의 관측된 NAT64 차단도 수정했으며 주소 4개 그룹 검사 및 R06 실제 PDF 다운로드·PDF.js 10페이지 추출을 확인했다. 이 PDF는 운영 DB 밖 검증 폴더에 원본 그대로 보존했다.

위 프로세스 실패를 조사하여 launcher/실제 Python의 PID·생성 시각·OS 핸들을 구분하도록 고쳤다. `spawn()`도 생성한 실제 자식 핸들의 Job 소속을 확인하고 미확인 자식은 종료·거부한다. 지원 범위는 앱의 고정 native 서버다. 최종 프로세스 검사 **19개가 가상환경 Python 26.522초·기본 Python 26.510초로 각각 통과**했다(skip 0, 경고 없음). Node의 강제 종료·stdin EOF, 실제 bridge 종료, 무관한 프로세스 보존을 확인했다. 앞선 전체 68개 통합 실행의 실패 기록과 이후 관련 검사 통과를 구분한다.

PDF 문단 변경 이전 소스 148개를 개인 데이터 없이 별도 폴더에 복사해 `npm run build`를 실행했고 컴파일·타입 검사·페이지 생성이 통과했다. 완료 당시 코드/설정 115개의 SHA가 원래 작업 폴더와 같음을 확인했다. `test-results/hymt-build-verification.json`에 기록했다. 실행 중인 앱의 빌드·서버·worker를 변경하지 않은 격리 빌드 검사이며 이후 PDF 변경의 최종 빌드와 구분한다. 두 lockfile 때문에 원래 저장소를 tracing root로 추정하는 Next.js 경고가 있었고 빌드 실패는 없었다.

PDF 변경을 포함한 격리 빌드도 `npm run build` 자체는 exit 0이었다. 다만 엄격한 helper는 Next.js가 `next-env.d.ts`의 두 import를 `.next/dev/types/`에서 `.next/types/`로 생성한 변경 때문에 effective exit 1을 기록했다. 최초 [실패 receipt](.training/verifications/hymt-build-final-1789021228540/build-execution.json)는 그대로 보존했다. [독립 바이트 대조](.training/verifications/hymt-build-final-1789021228540/generated-declarations-verification.json)에서 정확히 두 치환만 있음, 생성 선언 2개·BUILD_ID 존재, **검증 당시** 운영 코드·설정 133개와 격리본 나머지 입력 153개의 SHA 일치, 추가 PDF.js 자산 196개의 설치 패키지 바이트 일치를 확인했다. 검증 기록 SHA는 `1a3edc21536cfba466629f8444a7e19f2b766c86b301818443c78d053ec4c75e`다. 이 격리 빌드는 후속 `process_owner.py`의 원자적 Job 생성 수정을 포함하지 않으며, 새 코드의 실제 운영 빌드는 아래 기록과 구분한다. 최초 receipt를 통과로 덮어쓰거나 운영 서버를 재시작한 것은 아니다.

첫 PDF 문단 추출기 `pdf-paragraphs-v1`은 확실한 연속 줄만 문단으로 묶었다. 실제 NYU Session 2 PDF의 3페이지는 13개 줄 블록에서 7개 문단·제목·쪽수 블록으로 바뀌었고 전문·숫자·좌표·원본 SHA를 확인했다. 복잡한 7·9페이지는 병합을 멈추며 독립 표 라벨과 소수·음수 조각이 보존되는지도 대조했다. 당시 신규 검사 12개와 기존 추출 검사 3개, 타입 검사가 통과했다. 새 버전은 별도로 생성하고 기존 버전·블록·개인 기록·번역·캐시를 보존했다. 이 추출 검증과 후속 실제 번역 QA를 구분한다.

v1 실제 PDF 번역의 내용 추가를 대조한 뒤 `pdf-paragraphs-v2`를 구현했다. 안전한 미완결 글머리 도입과 2개 이상 하위 목록 전체를 묶으며 불일치 항목·6,000자 초과에서는 일부만 묶지 않는다. 실제 3페이지는 v1 7블록에서 v2 4블록, 원래 줄 수 `[1,4,7,1]`로 바뀌며 전문·기호·좌표를 보존했다. 복잡한 7·9페이지의 조각·순서·경고는 유지했다. [관련 검사 18개](.training/verifications/pdf-v2-contract-20260910.json)가 실제 PDF와 격리 DB의 v1 대기 작업·v2 저장·기존 메모/검수/캐시·설정 해시 보존까지 통과했다(제외 0, 약 5.146초). 기존 PDF.js TT32 경고는 남았다. 후속 v2의 새 PDF 4블록 실제 번역·저장·캐시 검증도 완료했으며 Q26·가중치·프롬프트·사전은 바꾸지 않았다.

[v2 실제 앱 QA](test-results/hymt-runtime-v2-verification-1789025037793.json)는 `2026-09-10T07:27:34.171Z`에 완료했다. 새 PDF 4블록의 실제 생성·저장·캐시와 기존 HTML 3문단의 원문·문맥·모델·번역·사용량 일치 및 캐시를 확인했다. 추가 HTML 추론은 0회였고 자동 검사는 모두 `passed`, `needs_review` 0개다. Root의 4블록 원문 대조에서 도입과 3개 조건이 모두 보존돼 v1의 실질적인 내용 추가가 해소됐으며 `자산 in place` 영어 잔류는 경미한 문제로 남았다. [독립 PDF v2 도우미 대조](.training/verifications/hymt-app-pdf-v2-assistant-review-20260910.json)도 4블록의 중요한 의미 오류 없음, 본문 1개의 경미한 완전성/정밀도 문제와 본문 2개의 문체 차이를 기록했다. 사람 검수는 아니다. v1 기록·운영 원문·개인 기록을 보존했으며 이 격리 QA 당시 제공자는 Argos였다.

이후 실제 [운영 빌드](.training/verifications/hymt-production-build-20260910.json)는 `2026-09-10T08:00:10.084Z`에 exit 0으로 완료됐다. `data/backups/2026-09-10T07-59-06-100Z-71b70e6b`의 일관된 DB·참조 파일 52개와 이전 `.next`를 `.training/verifications/pre-hymt-production-next-20260910/`에 보존했다. [제공자 선택 기록](.training/verifications/hymt-provider-selection-20260910.json)은 기존 112바이트에 없던 `TRANSLATION_PROVIDER=hymt` 항목만 추가하고 원래 바이트를 보존했음을 남긴다. 단일 웹·worker를 새로 시작한 뒤 [운영 HTML 검증](test-results/hymt-operational-html-1789027497099-402418fd-52a6-4044-a80a-ed95287bfee5.json)을 `08:14:10.018Z`에 완료했다. HTML 3문단을 새로 생성·저장하고 자동 검사와 캐시 3개를 확인했으며 유료 호출은 0개, 기존 기록·원본 파일은 모두 보존됐다. [최종 API·DB 상태](.training/verifications/hymt-final-operational-state-20260910.json)는 `08:16:12.246Z`에 `provider: hymt`, `configured: true`, `liveVerified: true`, API 키 불필요, 활성 작업 0개를 확인했다. 운영 검증 범위는 `html-only`이며 PDF 4블록의 성공은 앞선 격리 앱 QA와 구분한다.

운영 QA helper의 [첫 사전조건 실패](test-results/hymt-operational-html-1789027271165-bc690d1a-b11c-49fb-91f4-306c7eddc525.json)는 같은 Windows 경로의 `/`와 `\` 표기 비교 때문이며 번역 요청은 0개였다. [원래 helper](test-results/verify-hymt-operational-html-before-path-fix.mts)와 실패 기록을 보존한 뒤, 해석한 절대 경로와 실제 파일 SHA를 함께 검사하도록 고쳤다. 수정 helper SHA는 `62e5458d7b8df2c0f4b379de44131954a2341150151d0187258b17bc21e98499`이며 읽기 전용 검증·변조 4건 거부·엄격한 타입 검사를 통과했다. 앱 코드는 바꾸지 않았다.

[운영 HTML 도우미 대조](.training/verifications/hymt-operational-html-semantic-evidence-20260910.json)는 새 3개 원문·번역이 앞서 개별 검토한 쌍과 동일함을 확인했다. 핵심 의미는 보존됐지만 앞서 기록한 경미한 용어 정밀도·문체 문제까지 해소됐다는 뜻은 아니다. 이어 [표준 HTML 검증 CLI](.training/verifications/hymt-operational-metadata-1789028103209/report.json)는 exit 0으로 완료했고 추가 추론·작업·사용량·번역은 0개였다. 이전 운영 검증 metadata를 별도로 보존한 뒤 현재 모델의 `html-only` 검증 상태를 저장했다. [1440px·390px 홈/설정 화면 4개](test-results/hymt-readonly-ui-1789027442434/verification.json)도 읽기 전용 검사에서 통과했다. 이 화면 확인은 운영 추론 전 `liveVerified: false`였으며 최종 API의 `true` 확인과 시점을 구분한다. 비교용 2시간·앱 v2용 30분 임시 절전 방지 요청은 모두 해제됐다.

최신 [전체 앱 검사](.training/verifications/npm-test-pdf-v2-20260910.json)는 **93개 중 91 통과·2 제외·실패 0**이었다(약 44.184초). 제외한 opt-in 실제 PDF 검사 2개는 위의 별도 18개 검사에서 통과했다. `npm run typecheck`도 exit 0으로 완료했으며 임시 폴더 정리 보호의 별도 1개 회귀 검사도 통과했다. 모델 생성 품질이나 운영 활성화를 이 코드 검사만으로 보장하지 않는다.

PDF 변경을 포함한 전체 `npm test`는 **85개 중 83 통과·2 제외·실패 0**이었다(109.285초). 제외한 두 검사는 별도 실행에서 통과한 opt-in 실제 PDF 검사다. 실제 모델 앱 QA 도우미도 새 추출기·설정 해시·3페이지 7블록 전문과 순서를 검증하도록 갱신했고 전용 타입 검사가 통과했다. 후속 실제 앱 QA의 첫 시작 실패와 v1 생성·저장·캐시 완료는 위에 구분해 기록했다. 테스트 부하와 겹친 Q26 시간은 단독 성능으로 해석하지 않는다.

큰 CPU 모델이 긴 문단을 처리할 수 있도록 Hy-MT2 요청 한도를 실행기와 같은 30분으로 맞췄다. 시작 한도와 Argos·finetuned 요청 한도는 5분을 유지한다. 가상 시계로 5분을 넘긴 정상 응답 수신·30분 초과 시 대기 요청과 자식 정리·Argos 기존 한도를 확인했고 관련 번역 검사 **36개와 타입 검사가 통과**했다. Node 22.15의 MockTimers 실험적 API 안내가 있었으며 실제로 30분 동안 번역한 검사로 표시하지 않는다.

## 용어 표기 90% 목표 — v5 독립 최종 시험 93.64% 통과

사용자의 최신 목표를 절대 용어 표기 적중 90% 이상으로 기록했다. 기존 앱 사전의 54개 표기와 학습 1,152쌍(금융 992·일반 160), dev/test 각각 140쌍을 확정했다. 새 학습 예문 728쌍을 독립 전수 대조했고 재투자 금리/재투자율 같은 금융 내부 다의어도 구분했다. 기존 자료 중 표기가 일치하는 train 424쌍만 재사용한다. v4 선택 모델에서 이어 학습하되 과거 소비한 시험으로 조정하지 않는다. 금융 용어 90%·이전 모델 대비 회귀 없음, 숫자·일반 문장 유지, 빈 결과/상한 도달 없음, 일반 문맥의 금지 금융 뜻 출현 0개를 사전 기준으로 삼는다.

데이터·학습·평가·익명 대조 helper 159개 검사를 통과했다. 본학습은 1,728회 갱신·6 epochs를 완료했고, 고정한 dev 선택 순서에 따라 step 1728을 선택했다. 실제 tensor 253/255개가 변했고 선택 가중치 SHA는 `3132447ece0a80e30556cb1dce11fe4eb8c33244e56759600a2c4a6e6d019b86`이다. [v5 데이터·실행 보고서](content/training/FINANCE_V5_REPORT.md)에 단계별 점수·해시·명령을 보존했다.

독립 최종 시험 140개에서 용어 표기는 부모 **50/110(45.45%) → 103/110(93.64%)**, chrF 41.316315 → 48.860325, BLEU 14.478884 → 23.448432였다. 금융·일반 하위 기준을 포함한 사전 gate를 통과했다. 숫자 문자열 일치는 두 모델 모두 139/140이고, 한 건은 `3 times` → `세 번`의 같은 수량 표기였다. 빈 출력·상한 도달·일반 문맥에 주석된 금지 금융 표현은 0개다.

140개×두 모델의 익명 의미 대조 280개 판단을 완료했다. 도우미가 실질적인 의미 오류로 판정한 행은 전체 **79 → 50**, 금융 **63/108 → 32/108**, 일반 **16/32 → 18/32**였다. 예를 들어 새 모델의 reconciliation → 화해, share count → 주가 수가 남아 앱 기본 모델로 선택하지 않았다. 자동 gate 통과 기록을 바꾸거나 재학습하지 않았다. 사람 검수 자료는 0개다. 별도 긴 문단 네 구성 비교·96개 대조와 Hy-MT2 문맥 구성 등록은 완료했으며, 실제 앱 검증·활성화 상태는 위 절과 구분한다.

별도 `infer_quality_v5.py` 직접 CLI는 CPU FP32에서 실제 짧은 예문을 생성하고 exit 0으로 종료했다(입력 6·생성 5토큰, 생성 5.906초, 경고 없음). [실제 CLI 기록](.training/verifications/v5-direct-cli-20260910.json)에 남겼다. 확인된 토크나이저 저장 형식의 동등성만 별도 검증하고 동결된 `infer_v5.py`·가중치·최종 시험은 변경하지 않았다. 모델 준비·무결성 확인 시간은 생성 시간과 다르며, 이 결과는 Hy-MT2 앱 QA 통과 근거가 아니다.

## 실제 원문 도우미 번역 후속 학습 — v4 완료, 품질 추가 기준 미달

사용자는 별도 검수 자료가 없다고 확인했고, 도우미가 자료를 번역하여 학습시키는 방식을 승인했다. 저장된 불변 원문을 읽기 전용으로 가져와 실제 학습 번역 195쌍을 만들고 독립 도우미가 전수 대조했다. 부정 범위 등 4건을 고쳤고 원문 모호성 1건은 제외했다. 이전 train 200쌍·새 일반 40쌍과 합친 학습 435쌍, 문서 단위로 분리한 dev 48쌍·독립 test 42쌍의 숫자·원문 해시·분할 누수 검사에 통과했다. 사람 검수로 표시하지 않는다.

새 독립 평가·출처별 집계·데이터 출력 보존 검사를 포함한 Python 97개 검사를 통과했다. `finance-v4-teacher`는 실제 optimizer 327회 갱신 후 dev로 step 218을 선택했다. 최종 test 42개에서 용어 적중은 원본 33/102, v3 57/102, v4 60/102였다. 원본 대비 자동 기준은 통과했지만 v3 대비 사전 5%p 개선 기준에 미달했고, 블라인드 의미 대조에서도 오류가 남아 있다. 앱 기본 Argos와 운영 DB는 변경하지 않았다. [v4 보고서](content/training/FINANCE_V4_REPORT.md)에 해시·실행·실패 기준을 기록했다.

## 다의어의 무조건 금융 용어 치환 수정

단독 Compounding·Perpetuity·Sales·Regression 및 소문자 roe 등을 문맥 없이 금융 뜻으로 확정하던 경로를 수정했다. 실제 엔진 출력을 유지하고 검토 필요를 남긴다. 대소문자가 맞는 ROE와 명확한 복합 용어 처리는 유지한다. Argos의 실제 Python 처리 코드 해시를 캐시·실행 중 엔진 식별값에 반영하여 수정 전 캐시가 새 규칙의 결과로 재사용되지 않게 했다.

Python 런타임 검사 22개·Node 런타임 검사 7개와 TypeScript 검사를 통과했다. 별도 DATA_DIR 복원본에서 실제 Argos로 R01 HTML 3문단과 명시적으로 작성한 테스트 PDF 1페이지(2블록)의 번역·저장·캐시를 확인했다(`2026-09-09T23:57:22.181Z`). 메모·북마크·학습 진도·읽기 위치 보존도 확인했다. PDF는 내려받은 NYU 원문이 아니며 이 검증을 의미 품질 평가로 표시하지 않는다. 결과는 `test-results/polysemy-runtime-verification.json`에 있다.

## 새 번역 후보 실제 비교 — 2026-09-09

사용자의 추가 확인 요청으로 신규 미검수 탐색 자료 12개(금융 10·일반 2)를 세 모델에서 실제 번역했다. Argos 원시 출력·기존 finance-v3 CPU/FP32·TranslateGemma 4B 제3자 Q4_K_M CPU 출력 각각 12/12를 보존했다. 생성 시간 중간값은 0.422초 / 4.281초 / 21.125초, 지정 용어 표기 적중은 6/24 / 13/24 / 11/24다. 숫자 토큰 일치는 9/12 / 12/12 / 12/12이지만, 후보의 원문에 없는 억원 단위 추가·부정 반전·수식 변수 번역은 이 검사에서 잡히지 않았다. 자동 지표를 의미 정확도로 표시하지 않는다.

TranslateGemma의 최초 서버 시작은 구조화된 언어 필드를 요구하는 chat 템플릿 자동 해석 오류로 실패했다. 원래 GGUF 템플릿을 렌더링해 raw completion으로 전달한 뒤 Vulkan 첫 번역은 `ErrorDeviceLost`로 실패했다. 두 실패 로그를 보존하고 CPU 실행 12개만 완료 기록으로 집계했다. 모델·실행기 배포 해시, 입력·출력·템플릿·스크립트 해시, 속도·실제 번역문·자동 지표를 `.training/comparisons/`에 저장했다. 독립 CLI의 Python 구문 검사와 36개 출력 ID·원문 해시 대응·빈 결과/상한 여부를 확인했고 후보 자식 프로세스 종료를 확인했다. 앱 기능 변경이 없어 앱 테스트·HTML/PDF 저장·캐시 검증을 새로 수행한 것으로 보고하지 않는다.

새 가중치 학습·LoRA 검증·앱 제공자 전환·운영 DB 저장은 수행하지 않았다. 기존 학습/평가 기록과 사용자 변경은 보존했고 현재 앱은 Argos다. [비교 보고서](content/model-comparison/COMPARISON_REPORT.md), [실행 명령과 한계](scripts/model-comparison/README.md)를 참고한다.

## 번역 중 원형 로딩 표시

번역 버튼을 누르면 즉시 원형 회전 아이콘과 상태 문구가 표시된다. 요청 접수 이후에도 반환된 작업 ID와 현재 원문 버전의 실제 `queued/running` 상태에 따라 버튼·하단 상태·한국어 문단 표시를 유지한다. PDF 범위 번역의 반환 작업도 추적한다. 완료·일부 완료·실패·취소 상태에서는 해제하고 저장 번역을 재조회한다. 캐시만 반환하면 요청이 끝나는 대로 해제한다. 선택 해제·옆 패널 접힘에서도 하단 표시가 유지되며, 동작 줄이기 설정에서는 상태 문구를 남기고 회전을 멈춘다. 페이지 이동 전 요청의 늦은 블록 응답이 다른 위치를 덮어쓰지 않도록 확인한다. DB·번역 엔진은 변경하지 않았다.

최종 TypeScript 검사·프로덕션 빌드를 통과하고 로컬 웹을 재시작했다. API를 가로챈 UI 예제 검증 **10/10 통과**(2026-09-09T09:57:54.005Z): 지연 요청·대기·실행·완료·캐시·요청 오류·실패·부분 완료·취소·재진입·다른 버전 제외를 확인했다. PC/390px 화면과 동작 줄이기 설정도 확인했다. 실제 프레임 사이 회전 행렬이 변하고, 시작 알림이 버튼을 가리지 않는 것도 검사했다. pageerror·예상 밖 요청은 0개이며 이 UI 검증은 운영 DB나 실제 번역 모델을 호출하지 않았다. 결과는 `test-results/spinner-check.json`, 화면은 `spinner-running-desktop.png`와 `spinner-running-mobile.png`에 있다.

## finance-v3 당시 기록 — FP32 모델 제공 완료, 앱은 Argos 유지

최신 사용자 승인에 따라 공개 Marian 모델을 금융 영한 문장으로 실제 미세조정했다. `finance-v3`의 FP32 학습·최종 평가 기준은 통과했으며 선택 모델과 실제 로컬 추론 CLI를 제공한다. 첫 INT8 및 시작 임베딩 보존 v2의 dev 비교는 기준에 미달해 **해당 학습 모델의 앱 등록은 실행하지 않았고 당시 제공자는 Argos**였다. 추가 변형·설정 스윕·재학습 없이 이번 실험을 종료했다. [실제 학습 보고서](content/training/TRAINING_REPORT.md), [모델 카드](.training/runs/finance-v3/MODEL_CARD.md), [학습 도구 안내](scripts/model-training/README.md)에 근거와 보관 기준을 기록했다.

| 단계 | 확인된 상태 |
|---|---|
| 모델·환경 준비 | `.venv-training/`와 `.training/`를 사용. 기반 모델 `Helsinki-NLP/opus-mt-tc-big-en-ko`, 고정 revision `ae8606b7b29a495f31ce679cee2007f536a3a5ce`. 실제 전체 파라미터 211,223,552개, 학습 가능 209,126,400개 |
| 학습·평가 데이터 | 총 300쌍: train 200(금융 160·일반 40), dev 40(금융 30·일반 10), 독립 test 60(금융 40·일반 20). 도우미 작성 미검수이며 실제 사용자 검수쌍 0개에서 시작 |
| 기반 어휘 복원 | 원본의 한국어 vocab가 영어 source ID와 불일치하여 CPU/XPU에서 비정상 번역을 재현. `finance-v2`는 19 updates에서 중단하고 성과에서 제외. 원본·SPM·가중치를 보존한 별도 prepared 모델의 영한 어휘표로 해결. 가중치 학습 전후 같은 복원 토크나이저 사용 |
| 코드·빌드 검증 | 학습 helper Python 23개 + runtime helper 25개 = **48개 통과**(최종 실행 3.462초). Node **41개 통과**, TypeScript 최근 재검사·프로덕션 build 통과. 실제 모델 품질 평가는 아래 기록과 구분 |
| 본학습 | `finance-v3`, Intel Arc 140V XPU/FP32, 학습률 `1e-5`, **150 optimizer updates / 3 epochs 완료**. dev로 **step 100** 선택. 255개 tensor 중 **253개** 값 변화 |
| 학습 시간·손실 | 시작 2026-09-09T09:07:14.650745Z, 요약 완료 09:18:41.665515Z, 경과 687.014770초. epoch 평균 loss 1.559702 / 1.058853 / 0.849884. 선택본 dev token loss 1.273467 |
| 최종 평가 | 2026-09-09T09:23:35.272938Z 완료. `devGate`, `testGate`, `promotionEligible` 모두 true. 금융·일반 하위 기준 통과. 용어 보정·검수 메모리 적용 없음 |
| 원시 모델 추론 | 선택본의 CPU/FP32 CLI 단문 추론 성공. 실제 가중치 해시 일치, 모델 로드+생성 4.891초. `.training/runs/finance-v3/inference-smoke.json`. 단문 실행 확인이며 별도 held-out 점수로 합산하지 않음 |
| INT8 변환·비교 | 첫 변환의 금융·일반 BLEU 회귀로 실패. 시작 임베딩 보존 v2도 2026-09-09T09:42:32.427163Z에 dev 비교 완료 후 **금융 BLEU 회귀로 실패**, 일반 영역 통과. 추가 변형 없이 종료·기준 유지 |
| 앱 적용 | **등록 미실행, Argos 유지**. 새 finetuned 모델의 HTML/PDF·저장·캐시 브라우저 E2E는 아직 실행하지 않음 |
| 당시 Argos 앱 재검증 | 당시 build의 격리 E2E **18/18 통과**(2026-09-09T09:34:31.534Z). 실제 R01 HTML 103자와 자체 제작 PDF 1페이지 2블록 60자의 번역·캐시·검수·메모리·내보내기·오프라인·재시작 확인. pageerror 0개, console 400 1개는 의도적인 숫자 999 검수 거부. `test-results/browser-e2e.json`, provider Argos / en→ko 1.1 |

train/dev의 형식·고유 ID·숫자/통화·실제 용어 구간과 파일 해시를 검증했다. 독립 test와의 완전 중복·숫자 치환 중복은 0개다. 이러한 검사와 GPU 역전파 성공은 학습 파이프라인의 검증이며 일반 번역 품질의 검수 결과는 아니다. 학습·평가에는 용어 사후 보정이나 검수 메모리를 적용하지 않고 기반 모델과 같은 추론 조건으로 비교한다. 일반 문장 유지 기준도 dev 선택과 최종 평가에 포함한다.

| 고정 test | 문장 | chrF 전 → 후 | BLEU 전 → 후 | 용어 적중 전 → 후 |
|---|---:|---|---|---|
| 금융 | 40 | 29.736046 → 33.790239 | 2.922553 → 9.605595 | 15/48 → 25/48 |
| 일반 | 20 | 32.679527 → 35.942196 | 5.827112 → 9.313832 | 해당 없음 |
| 전체 | 60 | 30.668730 → 34.473414 | 3.910144 → 9.531970 | 15/48 → 25/48 |

전체 숫자 검사 일치는 두 모델 모두 **59/60**, 금융은 39/40, 일반은 20/20이다. 명시적 숫자가 있는 원문 5개 행은 5/5 보존했다. `during the year` → `1년 동안`으로 숫자를 도입한 `TEST-F03`은 불일치로 남는다. 빈 출력·상한 도달은 모두 0개다. 용어 적중은 지정 표기 확인이며 의미 정확도가 아니다. 도우미의 사후 의미 검토에서 현금 유입을 현금영수증으로 옮기거나 부정을 뒤집는 기존 오류와, 무형자산상각·지급 시점·FCFE 관계·덮는 동작의 개별 회귀가 남았다. 이를 보고 모델·데이터·평가 기준을 다시 조정하지 않았다.

첫 INT8 dev parity(2026-09-09T09:25:22.929383Z)의 금융 BLEU는 선택 FP32 15.777430 → 13.841892, 일반은 11.563406 → 10.026970이었다. 허용 하락 1점을 넘었으므로 앱 등록을 차단했다. dev 40쌍만 사용했고 최종 test는 재사용하지 않았다.

[읽기 전용 진단](.training/runs/finance-v3/parity-diagnosis.json)은 선택본에서 학습된 decoder 시작 pad 임베딩(L2 norm 0.01271195337176323)을 표준 CT2 변환이 버리고 0으로 시작하는 차이와 HF/CT2 beam 종료 규칙의 차이를 확인했다. CPU INT8와 XPU FP32의 수치 차이도 있으며 각 영향은 분리하지 못했다. 따라서 첫 실패를 양자화만의 문제로 단정하지 않는다.

새 기본 경로 [export_model.py](scripts/model-training/export_model.py)로 학습된 시작 임베딩을 보존한 v2를 생성했고 [v2 최종 비교](.training/runs/finance-v3/parity-summary-v2.json)를 dev 40쌍으로 한 번 실행했다. 금융 BLEU는 15.777430 → **14.666549**(하락 **1.110881**, 허용 1.0)여서 실패했으며 일반은 11.563406 → **11.492243**로 통과했다. 금융 chrF는 43.817244 → 43.644233, 일반은 38.767594 → 38.767167이다. 용어 14/31 → 15/31, 전체 숫자 검사 40/40 → 40/40, 명시적 숫자 행 1/1 → 1/1, 빈 결과·상한 도달 0개였다. 문자열 일치는 첫 변환 24/40에서 v2 **27/40(67.5%)**로 바뀌었지만 금융 회귀 기준을 충족하지 못했다.

첫 변환의 원래 파일을 유지하고 `deployment-attempts/int8-v1/`에도 15개 파일을 해시 검증해 복사했다. [보존 명세](.training/runs/finance-v3/deployment-attempts/int8-v1/preservation-manifest.json), [v2 변환 명세](.training/runs/finance-v3/export-manifest-v2.json)를 각각 남겼다. 고정 train.py·가중치·최종 test·판정 기준은 바꾸지 않았고 test를 변환 조정에 재사용하지 않았다. 시작 임베딩 누락은 고쳤지만 잔여 beam·연산 차이의 기여는 미확정이다. 추가 변형·설정 스윕·재학습 없이 FP32 모델 제공·앱 Argos 유지로 종료했다. 새 finetuned 앱 E2E 성공을 주장하지 않는다.

원저작자는 Helsinki-NLP / 헬싱키대학교 Language Technology Research Group이며 모델 라이선스는 CC BY 4.0이다. 상세 저작자 표시·수정 사실·가중치와 데이터 해시는 학습 보고서와 모델 카드에 있다. `.training/`의 학습 가중치·optimizer·체크포인트·평가 기록은 SQLite 백업에 포함되지 않으므로 별도로 보관한다.

## 금융 용어 보강·검수 번역 메모리 — 앱 v3 기록

- 다모다란의 공식 용어집·지표 정의·재무제표 입문 등 6개 출처에서 기준 표현을 확인해 교정용 규칙 **47개**를 등록했다. 42개는 본문 복합구, 5개는 제목·셀 전체 일치다. 기존 학습용 용어 44개는 유지한다. 한국어는 학습실 번역 정책이며 다모다란의 공식 한국어 번역이 아니다. [규칙·출처·관측 목록](content/translation-glossary.md).
- bridge v3는 문장을 먼저 번역하고 원문의 용어와 실제로 관측한 한국어 오역이 유일하게 대응할 때만 보정한다. 25개 규칙에 관측 후보 28개를 연결했다. 긴 복합구 우선·조사·서술격·띄어쓰기와 충돌을 처리하며, 약어·다의어의 무조건 본문 치환은 하지 않는다. 모호하거나 빠진 용어는 삽입하지 않고 검토 경고를 반환한다.
- 동일 Argos 모델의 기본 문장 번역과 보정을 비교한 50개 표본(평가용 문장/정확 용어·제목 47개 + 실제 R01 3문단) 중 33개 결과가 바뀌고 12개는 검토 경고를 남겼다. 표본을 교정 후보 선정에 사용했으므로 독립적인 번역 품질 점수나 오역률로 해석하지 않는다. `.translation/term-evaluation.json`에 원문·전후·경고·출처 구분·모델/규칙 해시를 기록했다.
- 관측 예: `금융 진술은 …` → `재무제표는 …`, `잔액 시트는 …` → `재무상태표는 …`, `운영 소득` → `영업이익`. 같은 문장의 다른 오역은 남을 수 있다. 이 Argos 용어 보정에서는 모델 가중치를 변경하지 않았다.
- 리더에 HTML·PDF 텍스트 문단의 **번역 수정 → 검수 완료로 저장**을 추가했다. 숫자·수식 오류와 오래된 편집본 충돌을 거부하며 오류 시 초안을 유지한다. 검수 이력·기계 번역 원본은 별도 보존한다. 표 셀 편집과 전체 이력 탐색 UI는 후속 범위다.
- 원문·이웃 문맥·용어집·블록 종류가 정확히 같은 검수본만 새 원문 버전에서도 재사용한다. `origin=memory`로 표시하고 추가 추론·사용량을 만들지 않는다. `npm run export:translation-memory`로 검수 문장쌍을 로컬 JSONL로 내보낼 수 있다. 검수 메모리 자체는 자동 모델 학습이나 새로운 문장에 대한 일반화 기능이 아니며, 위의 승인된 Marian 미세조정은 별도 명령으로 실행한다.
- 스키마 2에 검수 이력·연결 테이블을 추가했다. 운영 마이그레이션 전에 52개 참조 파일을 포함한 자동 백업을 생성했다(`data/backups/2026-09-09T08-17-08-752Z-39aaeadc`). 변경 직후 이전 원문 버전·블록·번역·메모·북마크·진도·읽기 위치의 모든 행이 백업과 같고 외래키 오류가 없음을 확인했다. 기존 v1 백업의 복원·마이그레이션도 지원한다.
- Node 검사 **32개 통과**: 검수 이력·충돌·숫자/수식·캐시·메모리·백업/복원·마이그레이션, 동일 반복 문단의 블록별 캐시 분리, 번역 중 검수 완료와 새 현재 번역 생성의 동시성 회귀를 포함한다. Python 회귀 **17개**, 실제 NDJSON 프로토콜 **7개**, 설치 무결성 검사를 통과했다.
- 검수 UI fixture **6개 통과**: 키 없는 상태의 검수, 표 제외, 중복 저장, 숫자 오류, 동시 수정 충돌, 모바일·키보드 동선.
- 최종 프로덕션 빌드·타입 검사 통과. `npm run verify:translation:html`로 운영 R01 3문단의 최종 v3 번역·저장·캐시를 확인했다(2026-09-09T08:27:55.682Z). 원문·기존 번역은 보존했다.
- 최종 빌드의 실제 DB·브라우저 E2E **18개 통과**. 격리 복원 DB에서 R01 실제 번역, 자체 제작 PDF 페이지 번역, 숫자 오류 검수 거부와 초안 보존, 정상 검수 저장, 원래 기계 번역 행 보존, 다른 PDF 자료의 같은 본문에 검수본 재사용·추가 처리 0, 검수 JSONL의 문장·출처, 오프라인·재시작 후 보존을 확인했다. PDF 전체가 아니라 검수한 동일 본문 블록을 대상으로 재사용을 확인했다. 새 용어 규칙으로 NYU 원격 PDF를 가져온 검증은 아니다.
- 결과는 `test-results/browser-e2e.json`, 실제 화면은 `test-results/e2e-reviewed-pdf-translation.png`, `test-results/e2e-reviewed-memory-mobile.png`다. PC·390px 모바일 화면을 확인했다. JavaScript pageerror는 0개, console의 400 오류 1개는 의도적으로 틀린 숫자의 검수 저장을 거부한 요청이다. 최종 실행 이전 빌드 교체로 중지한 테스트는 완료 결과에 포함하지 않았다. 임시 DB·테스트 서버는 정리했고 운영 DB에 시험 검수본을 넣지 않았다.
- 이 단계의 운영 앱은 `http://127.0.0.1:3000`에서 Argos·용어 규칙 47개·API 키 불필요 상태로 실행했다. 당시 최종 백업은 `data/backups/2026-09-09T08-28-55-550Z-4fa6bf4e`이며 참조 파일 52개와 스키마 2 DB를 검증했다. 당시 운영 검수 문장쌍은 0개였으며 사용자가 검수 완료로 저장할 때부터 축적한다.

## 무료 로컬 번역 전환 기준 — v2 당시 기록

- 기본값은 `TRANSLATION_PROVIDER=argos`이며 API 키가 필요 없다. `npm run setup:translation`이 `.venv-translation/` Python 환경과 `.translation/`의 공개 영어→한국어 모델 1.1을 준비한다. 최초 다운로드에는 네트워크가 필요하다.
- 단일 worker가 Python 하위 프로세스와 NDJSON으로 통신한다. 설치 후 번역은 PC에서 처리하며 별도 번역 HTTP 서버를 운영하지 않는다.
- DB·원문·개인 기록·기존 번역을 보존한다. 실제 모델 해시·런타임 식별값으로 캐시를 구분하고 제공자 전환 후 이전 유료 대기 작업을 자동 실행하지 않는다.
- 로컬 글자 수·처리 단위와 선택형 API 토큰을 구분한다. 기본 분량 한도는 PC 자원 보호용이다.
- Argos 용어집은 정확히 일치하는 문단·셀에 적용하며 지정한 재무제표 복합구와 제목 전체 일치를 보완했다. 숫자·수식을 모델 밖에서 분리 보존하므로 문장 자연스러움에 제약이 있을 수 있다. 금융 전문 검수 완료로 표시하지 않는다.
- 최종 확인 대상: 실제 설치·런타임/모델 무결성, HTML 3문단·원격/업로드 PDF 1페이지 번역·저장·캐시, 외부 전송 없는 로컬 실행, 새 제공자 기준 자동 검사·브라우저 동선. `--html-only` 성공은 해당 범위로만 보고한다.

도구와 모델 출처는 [Argos Translate 공식 저장소](https://github.com/argosopentech/argos-translate), [공식 모델 인덱스](https://raw.githubusercontent.com/argosopentech/argospm-index/main/index.json)다.

## 무료 번역 전환 후 확인 — v2 당시 기록

- Python 3.11·Argos 1.11.0·CTranslate2 4.8.2·SentencePiece 0.2.2·spaCy 3.8.16 설치. 66개 의존성을 `scripts/local-translation/requirements.lock.txt`에 기록했고 `pip check`를 통과했다.
- 모델 압축 파일 120,789,009바이트, SHA-256 `e03d8e65e6d44525ec5808c3409fcf8728c76c2c76925372b6d3dc3278de17fc`. 실행 전에 파일 목록·크기·해시·런타임 버전을 확인한다. 이 절의 검증 당시 bridge는 v2였다.
- 실행 환경 약 1.03 GB와 모델·설치 파일 약 0.26 GB, 합계 약 1.28 GB. `setup:translation` 재실행은 설치 무결성을 확인하고 종료했다.
- Node 자동 테스트 **22개 통과**. 새 제공자의 캐시 분리·이전 유료 작업 거부·로컬 사용량과 API 토큰 구분 및 영숫자 식별자 보호를 포함한다. 최종 프로덕션 빌드와 TypeScript 검사도 통과했다.
- Python 회귀 **7개 통과**. 보호 표식·수학 기호·용어 범위·모델 변조·네트워크 차단과 재무제표 복합구를 확인했다. NDJSON 요청·ID 거부·원문 비노출 오류·EOF 종료 등의 프로토콜 검사 7개도 실행했다.
- 실제 R01 3문단(650/209/308자) 번역에 v2 기준 약 2.31/1.77/0.33초. 첫 모델 로드는 약 4초인 실행과 222초인 실행이 모두 관측되어 성능 상한을 보장하지 않는다. 시작 준비와 개별 요청의 제한은 각각 300초다.
- `npm run verify:translation:html`로 실제 운영 DB의 R01 3문단을 번역·저장하고 재요청 시 추가 처리 없이 캐시를 확인했다. `data/derived/translation-verification.json`의 범위는 `html-only`다.
- 최종 빌드의 UI fixture 검사 **5개 통과**. 무료 준비/미설치 상태, 선택한 문단 요청, 390px 모바일, 선택형 OpenAI 구분을 확인했다. 모바일 메모 패널은 처음 진입할 때 접힌다.
- 실제 PDF 시험에서 `E2E`가 훼손되어 검토 필요로 표시되는 문제를 재현했다. 영숫자 식별자를 통째로 보호하도록 고치고 처리 규칙을 `argos-finance-ko-v2`로 올렸다. 변경 전 결과를 새 유효 캐시로 표시하지 않으며 이전 규칙의 대기 작업은 현재 규칙으로 새로 요청해야 한다.
- 최신 규칙의 실제 브라우저 E2E **16개 통과**, pageerror·consoleerror 0개. 격리 복원 DB에서 R01 새 문단 103자와 자체 제작 PDF 1페이지의 2블록 60자를 UI로 번역하고 저장·숫자/영숫자 보존·캐시·외부 API 사용량 증가 없음·오프라인 저장 번역 읽기·웹/worker 재시작 후 보존을 확인했다. PDF는 명시된 테스트 예제이며 NYU 원격 PDF 성공을 뜻하지 않는다.
- 최종 실제 화면은 `test-results/e2e-argos-html-translation.png`, `e2e-argos-pdf-translation.png`에 남겼다. 결과와 출처·검증 범위는 `test-results/browser-e2e.json`에 기록했다. 임시 DB·PDF는 정리했고 운영 메모·진도에 테스트 기록을 넣지 않았다.
- 운영 서버에서 provider=argos, configured=true, API 키 불필요, 현재 모델/처리 규칙 검증 기록 있음 상태를 확인했다. 운영 R01에는 유효한 번역 3문단이 저장되어 있다. 원격 번역 처리량은 0이다.
- 의미 검수 통과를 주장하지 않는다. 예를 들어 일반 본문 속 `primer`를 ‘뇌관’으로 옮기는 오역이 남는다. 재무제표 복합구 보완이 모든 문장의 자연스러움을 해결한 것은 아니다.

## 구현한 기능

| 영역 | 구현과 저장 동작 |
|---|---|
| 홈·탐색 | 실제 최근 읽기, 저장한 자료, 단원 진도, 자료 검색·유형·단계·우선순위 필터 |
| 학습 경로 | 단원 8개, 선수 용어·목표·읽을 자료·확인 질문·관련 도구, 직접 체크하는 진도 |
| 자료·도구 | 초기 자료 36개(R 20/B 6/T 10), Excel 가이드 10개, 원문·관련 자료·단원 연결. 외부 원문을 달모다란에 보관하는 동작과 저장된 보관본을 PC로 다운로드하는 동작을 방향·문구·아이콘으로 구분 |
| 금융용어 | 한국어·영어·약어·별칭으로 검색하는 용어 44개 |
| HTML 읽기 | 버전별 문단·표·이미지, 원문/한국어/나란히, 블록 기준 위치·메모·북마크 |
| PDF 읽기 | 업로드 원본 보존, 별도 worker 추출, PDF.js canvas·텍스트 레이어·페이지·확대·range 응답 |
| 번역 | 선택 범위·ID/숫자/수식 검사·캐시·검토 필요·사용량. 기본 제공자는 설치한 무료 Argos이며 OpenAI Responses API는 명시 선택 옵션 |
| 작업 처리 | SQLite 영속 큐, lease·heartbeat, 중복 범위 재사용, 부분 실패·취소·제한 재시도·중단 복구 |
| 개인 기록 | SQLite에 메모·북마크·진도·버전별 읽기 위치 저장, 편집·삭제·다음 방문 복원 |
| 설정·운영 | 글자 크기·줄 간격·읽기 모드, 비밀값 없는 번역 연결 상태, 백업·별도 폴더 복원 |

11개 화면 경로는 `/`, `/learn`, `/learn/:slug`, `/library`, `/resources/:id`, `/reader/:id`, `/tools`, `/tools/:slug`, `/glossary`, `/notes`, `/settings`다. 자료실과 도구실은 같은 Resource를 사용하며, 읽기 URL의 versionId와 문단·페이지가 메모의 원문 버전을 지정한다.

## 실제 원문 확인

| 대상 | 결과 |
|---|---|
| R01 재무제표 입문 | 실제 HTML·본문 이미지 저장, 표 5개와 본문 순서 보존 |
| R02 현재가치 입문 | 실제 HTML·본문 이미지 저장, 표 3개와 긴 비교표·위/아래첨자 보존 |
| R05 가치평가 입문 | 실제 HTML·본문 이미지 저장, 본문 순서 확인 |
| T01 Excel | 실제 원본 62,976바이트 저장; 원본 전달 응답과 DB SHA-256 일치 확인 |
| R06 목록의 대표 PDF | 목록에서 확인한 첨부 링크를 R06-PDF02로 등록하고 다운로드 시도. 리디렉션 후 최종 서버 시간초과. 실패 작업 보존 |
| PDF 업로드 | 독자 제작한 2페이지 `TEST FIXTURE`를 격리 테스트 DB에 업로드해 실제 worker·PDF.js로 검증. Damodaran PDF로 표시하지 않으며 운영 자료실에 예제로 심지 않음 |

원문 목록은 첨부 PDF 메타데이터를 포함해 현재 37개다. 실제 가져온 파일이 37개라는 의미는 아니다. R01·R02·R05는 `structured-v2` 추출기로 다시 저장했고 이전 버전도 보존했다. 원문과 비교하여 본문 글자·숫자 순서가 일치하는지 확인했다.

NYU의 www → people → pages 리디렉션을 실제 확인해 people.stern.nyu.edu를 허용 목록의 명시적 추가 설정으로 등록했다. 원문 다운로드와 이미지마다 호스트·경로·DNS·최종 형식을 검사한다. PDF 요청 실패를 성공 파일로 저장하지 않는다.

## 실행한 검증 — Argos 전환 전

다음은 기존 앱에서 실제 실행한 검사다. Argos 변경 후 재검증 결과와 로컬 엔진의 성능·품질 결과는 별도로 기록해야 한다.

- Windows PowerShell에서 설치·setup·웹·별도 worker 실행. 최종 `npm run typecheck`와 `npm run build` 통과.
- `npm test`: **19개 통과**. seed 재실행 후 개인 기록 보존, 참조 소유권, 실제 HTML/PDF 추출, 번역 응답 손상·겹치는 범위·캐시·429·취소·lease 복구, 분량 한도, 백업 누락·변조·복원 검증.
- `npm run test:smoke`: PC 1440px·모바일 390px에서 주요 11개 화면 확인. 검사 21건과 최초 홈 진입 성공, 문서 가로 넘침·브라우저 JavaScript 오류 없음.
- `npm run test:browser`: **14개 통과**, pageerror·consoleerror 각각 0개. 별도 복원 DB에서 검색·사전·북마크·진도·HTML 문단 메모 저장/수정·버전/위치 복원·PDF 업로드/추출/렌더링·원본 해시·모바일 메뉴·재시작·삭제 동작 확인. 결과는 `test-results/browser-e2e.json`에 기록했다.
- 외부 네트워크를 차단한 브라우저에서 저장 HTML·PDF·메모 열기 성공, 외부 요청 0개. 실제 저장 번역의 오프라인 화면은 라이브 번역 데이터가 없어 별도로 확인하지 못했다.
- 실제 HTTP로 Origin 누락·외부 Origin·외부 Host를 403으로 거부, 타 자료의 버전 요청·없는 단원을 404로 거부, 잘못된 byte range를 416으로 거부하는 것을 확인했다. 결과는 `test-results/api-smoke.json`에 기록했다.
- `npm run backup`: 실제 운영 DB 스냅샷과 참조 파일 **51개** 백업 성공. 테스트에서는 별도 경로 복원 후 메모·진도·위치·원본 해시와 외래키 무결성 확인.
- 이전 OpenAI 기본 구성의 `npm run verify:translation`: 키·모델 없음 안내와 실패 종료 코드 확인. 외부 번역 호출·유료 사용량 없음. 이 결과는 후속 Argos·Hy-MT2 로컬 구성의 번역 검증 결과가 아니다.

브라우저 산출물은 `test-results/home-desktop.png`, `home-mobile.png`, `e2e-html-reader.png`, `e2e-pdf-reader.png`, `e2e-mobile-records.png`다. 테스트 중에만 작성한 메모·진도·PDF는 임시 DATA_DIR 안에 두며 검증 후 제거한다. 단순 화면 smoke는 운영 reader를 방문하므로 최근 읽기 위치를 갱신할 수 있다.

자동 테스트용 번역 제공자로 응답 검증·캐시·작업 흐름을 확인했으며 실제 한국어 번역의 의미·품질을 검증한 것은 아니다. 운영 DB에 가짜 번역을 넣지 않았다.

## DB와 운영 결정

- Next.js App Router + Node.js, React, TypeScript, SQLite, PDF.js를 사용한다. Node 패키지 버전은 package-lock.json에 고정했다. 최신 번역 구성은 Argos Python 하위 프로세스가 기본이며 공식 OpenAI SDK는 선택형 제공자로 유지한다.
- SQLite는 better-sqlite3 13.0.3의 실제 런타임 3.53.4다. WAL·외래키·busy timeout 5초·FULL 동기화와 짧은 트랜잭션을 사용한다.
- 초기 Drizzle 후보 대신 명시적 SQL 마이그레이션·파라미터 SQL로 복합 참조·작업 claim·백업을 관리한다. 적용한 마이그레이션은 변경하지 않는다.
- 웹은 127.0.0.1에 바인딩하며 API에서 Host와 상태 변경 Origin을 검사한다. 키는 서버 환경에서 읽고 브라우저에 설정 여부만 제공한다.
- 원본·로컬 이미지·DB·키는 Git/public에 포함하지 않는다. PDF.js 실행 자산은 로컬에서 제공한다.
- 복원은 새 DATA_DIR에서 검증한다. 대기·실행 작업은 취소 상태로 복원해 로컬 처리와 선택형 외부 호출이 자동 재개되지 않도록 한다. 재설치 가능한 Python 환경·모델은 개인 데이터 백업에 포함하지 않는다.
- Windows 실행기는 자신이 시작한 웹·worker 프로세스를 함께 종료한다. 테스트 재시작으로 확인했다.

## 외부 제약과 남은 검증

1. **번역 품질·속도:** 무료 로컬 번역은 설치·실제 HTML·캐시를 확인했다. 일반 금융 문장에는 오역과 어색한 연결이 남으며 미검수 초안으로 제공한다. 최초 모델 열기가 느릴 수 있다. `npm run verify:translation:html`은 HTML 범위만 확인하는 명령이다.
2. **대표 원격 PDF:** 원문 서버 연결이 회복되면 실패 작업을 재시도한다. 지금은 업로드 경로로 PDF를 읽을 수 있으며 원격 PDF 성공을 보장하지 않는다.
3. **복잡한 원문:** 스캔 PDF는 OCR 필요 상태로 남는다. PDF 다단 편집·수식은 원본 페이지와 함께 확인해야 한다. 같은 HTML 안의 문제·해답을 안정적으로 분리할 수 없는 경우 통합 원문으로 제공한다.
4. **자료 범위:** 모든 아카이브를 수집하지 않았다. Excel 10개 가이드는 목적·입력·해석·링크를 제공하며 확인하지 않은 시트/셀 위치는 명시한다. 각 자료는 사용자가 선택해 가져온다.
5. **선택형 OpenAI:** 사용하려면 `TRANSLATION_PROVIDER=openai`, `OPENAI_API_KEY`, `TRANSLATION_MODEL`을 명시하고 웹·worker를 재시작한다. 이 경우 실제 API 사용량이 발생하며 원문·선택 문맥·관련 용어가 외부로 전송된다. 미설정은 선택형 API만의 제약이다.

P1인 OCR, 자유 질의 AI 해설, 전체 번역 수정 이력 탐색 UI, 자동 채점·오답 노트, 한국어 PDF 내보내기는 구현 범위에 포함하지 않았다. 문단 번역 수정·검수 저장·이력 보존과 공개 Marian 모델의 실제 금융 미세조정은 사용자 추가 승인 범위다. 미세조정은 위의 진행 단계로 구분하며 실시간 시세·매매 추천·회원가입·공개 배포를 추가하지 않았다.


## 2026-09-12 17:09 KST — 읽기 재시도 사전검사 보류

읽기6의 저장 결과는0개이며, 전원/절전/연결 변화 뒤 프로세스가 사라져 정상 summary가 없다.49파일과 프로세스 부재를 보존했고 종료 코드/주체는 미확정으로 남겼다. 실행 사건1개 추가·반복0/재사용1로 원장은738개다. 전원 재연결 뒤 새 읽기6 coordinator를 시작했으나 물리 메모리 최소10.64GiB와 첫 조회 시간 초과 때문에 사전검사가 DEFERRED로 종료됐다. 모델 로딩0, 일시적 전원 요청 해제, 소유 wrapper 없음 상태를 확인했다. [인수인계27절](TRANSLATION_HANDOFF_20260911.md#27-읽기6-중단-증거와-재시도-사전검사-보류)에 실제 상태와 재개 조건을 기록했다. 현재 모델 실행은 없고 읽기6·독립 질문은 미완료다. 새 코드는 기존 coordinator의 입력/출력 범위만 읽기6으로 바꾼 별도 실행 스크립트이며 AST/차이 검토와 실제 사전검사 거부를 확인했다. 앱 테스트나 새 엔진 완료로 보고하지 않는다.
