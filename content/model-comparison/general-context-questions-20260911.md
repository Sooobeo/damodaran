# general16 한국어 이해 질문 — 2026-09-11

[동결 질문·정답 JSON](general-context-questions-20260911.json)은 16항목 × 2문항, 총 32문항이다. 각 항목의 Q1은 핵심 관계, Q2는 부차·결합·조건 확인으로 구성했다. 핵심 문항은 16개다. 원문 의미 요건·가능한 답·허용 동의 표현·영어 근거 구절·원문/문맥/한국어 학습 문맥 해시를 포함한다.

- 정책: [금융자료 학습 준비 기준](LEARNING_READINESS_BASELINE_20260911.md), `learning-readiness-20260911-v1`.
- 질문 버전: `general-context-questions-20260911-v1`.
- 동결시각: `2026-09-11T03:21:08.626318+00:00`.
- 질문 JSON SHA-256: `4faac811b4b2644fe73f2b2adcfa9862207243988e963f4e7d331d627900ed51`.
- 원본 dataset SHA-256: `4d63cbbd7299835849cdf750813e7c18a051ae98149a8ec51b73d5e8057cdfd4`.

작성·자체 점검 중 general16 후보 출력은 열람하지 않았다. 원문·문맥을 우선했고 기존 manifest의 원문 점검 메모도 보았다. `referenceKo`와 `checks`의 값은 질문 작성에 열람·사용하지 않았다. 작성자는 다른 번역 검토 경험이 있으며, 질문 자체 점검은 독립 검토나 사람 검수가 아니다.

## 두 프로필에 같은 학습 문맥

GCTX26-013~016의 4개에만 원래 영어 문맥을 충실히 옮긴 한국어 문맥을 허용한다. raw/contextual **답변자에게 같은 한국어 문맥**을 제공한다. 나머지 12개에는 문맥을 추가하지 않는다. 이는 평가 조건이며 기존 모델 입력·데이터셋·생성 프로필을 바꾸지 않는다.

문맥에는 원문 `context`에 없는 방 예약, 실제 교정 범위, 일정 조정 결과, 다툼 이후 관계·작업 결정을 보충하지 않았다. 각 질문은 대상 번역에만 있는 필수 사실을 포함한다. 예를 들어 방문객의 관심만 답해서는 013의 공간 준비 질문을 맞힐 수 없고, 015에서 모임의 여건 차이만 되풀이해서는 위원회의 실제 조치와 목적을 맞힐 수 없다. 이 점검은 문맥만으로 정답이 논리적으로 제공되는지를 본 것이며, 사람이 추측으로 맞힐 확률을 측정한 실험은 아니다.

이 4개 점수는 **번역+사전 한국어 문맥의 답변가능성**이다. 문맥 없는 번역 단독 이해 점수로 표시하지 않는다. 문맥을 이용해 번역의 오역을 고쳐 읽어 정답을 맞혔더라도 번역 자체의 major/critical 오류는 별도 의미 gate에서 처리한다.

## 답변과 채점 분리

1. 정답과 원문을 보지 않은 별도 답변자에게 `questionKo`, 해당 후보 한국어 번역, 허용된 `learningContextKo`만 준다. 영어 원문·영어 문맥·참조 번역·정답 키·다른 후보 출력을 함께 주지 않는다. 이 JSON 전체를 답변자에게 전달하면 안 된다.
2. 답변자는 답과 번역/한국어 문맥의 근거, 정보 부족·모호함을 먼저 저장한다. 후보별 답변 세션을 분리한다. 정답을 아는 질문 작성자는 같은 문항의 번역만 답변자 역할을 맡지 않는다.
3. 답변을 고정한 뒤 채점자가 원문과 정답 의미 요건을 대조한다. 자연스러운 동의 표현·이름 표기·어순 차이를 허용한다. 예시 답은 유일한 정답 문자열이 아니다. 질문 하나는 필수 의미 요건을 모두 충족해야 하며 gate용 부분점수는 없다.

모델·구성·프로필마다 원래 분모 32개를 유지한다. 질문 기준은 전체 최소 31/32, 핵심 16/16이며, 미완료·모호함·분쟁이 남으면 통과로 선언하지 않는다. general12와 문맥4, 핵심과 보조 문항 결과도 따로 보고한다. Q2의 `criticalQuestion=false`가 해당 내용의 major 오류를 허용하는 것은 아니다. 질문 점수가 원문 중심 major/critical 0건 등 다른 필수 gate를 대신하지 않는다.

원문에 없는 냉각 시간, 방문객 확인의 구체적 방식, `aunt`의 모계·부계·혼인 관계 등을 억지로 정답화하지 않았다. 질문 결함이 나중에 발견되면 원래 분모와 결과를 보존하고 새 버전으로 기록한다. 후보 출력 관측 후 이 파일을 맞춰 고치지 않는다.

## 확인한 범위

항목 16개·고유 질문 32개·핵심 16개, 근거 54구절의 실제 source/context 포함 여부, 모든 원문·문맥 해시와 한국어 문맥 4개를 확인했다. 기존 dataset은 변경하지 않았다. 실제 답변 평가·모델 실행·독립 최종 시험·인간 학습 효과 검증은 하지 않았다. 이 32문항이 모든 명제·용어·오류를 빠짐없이 덮거나 금융·일반 언어 전체를 대표한다고 주장하지 않는다.
